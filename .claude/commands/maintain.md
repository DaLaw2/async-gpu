# Codebase Maintenance

Dispatch maintenance sub-commands: ci, archive, readme, nightly, patches, gitignore.

Usage: `/maintain <sub-command>` or `/maintain all`

Launch: `Workflow({ name: "maintain", args: { commands: "$ARGUMENTS" } })`
