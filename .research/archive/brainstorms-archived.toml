# Archived brainstorms — moved from state.toml at cycle 193
# Keeping last 3 (bs47, bs48, bs49) in state.toml

[[brainstorms]]
seq = 41
at_cycle = 163
trigger = "User demanded autonomous iteration + generation produces garbage text"
level = "deep"
key_insight = "Stop waiting for PyTorch. Self-diagnose: (1) WebFetch confirms GPT-2 predicts Paris, (2) weight transpose is #1 suspect (Conv1D [in,out] common bug), (3) per-layer diagnostics find where semantics are lost, (4) LM head position audit. Model limitation <5% — top-5 being function words is pathological."

[[brainstorms]]
seq = 42
at_cycle = 167
trigger = "Brainstorm interval (4 tasks completed since last bs)"
level = "standard"
key_insight = "GPU inference now correct (gemm_f32 matches CPU f64). NaN at step 22 is next blocker — online softmax is the fix. 4 themes completed. 'Paris' criterion unreachable (model limitation), suggest user revision."

[[brainstorms]]
seq = 43
at_cycle = 169
trigger = "New gpu-perf epic + user requests cleanup/examples + Product Ready"
level = "standard"
key_insight = "MMA bug likely host-side grid_dim or bar_sync scope, not fragment indexing. KV cache design can parallel with MMA fix. repo-cleanup (PTX gitignore + model dedup + examples) is independent work."

[[brainstorms]]
seq = 44
at_cycle = 171
trigger = "Brainstorm interval (3 tasks completed since bs43)"
level = "quick"
key_insight = "MMA kernel correct but f16 insufficient for 12-layer inference (top-5 diverges). 1.8x speed gain confirmed. mma-fix theme completed. Next priority: KV cache (gpu-perf) and examples (public-api) in parallel."

[[brainstorms]]
seq = 45
at_cycle = 174
trigger = "User request: add mixed-precision/BF16 to gpu-perf epic, prioritize KV cache"
level = "quick"
key_insight = "Added mixed-precision theme (BF16 MMA) to gpu-perf. Parked host-sdk (host-sdk.3 blocked by missing host-sdk.2). Priority: kv-cache.2 first, then mixed-precision.1."

[[brainstorms]]
seq = 46
at_cycle = 177
trigger = "Brainstorm interval (3 tasks completed since bs45)"
level = "quick"
key_insight = "mixed-precision.1 done: BF16≈F16 precision (max divergence 0.2042). Proceed with mixed-precision.2 (inference validation). Expect same outcome as f16 MMA inference (top-5 divergence). public-api epic needs attention after mixed-precision completes."

# --- Archived at cycle 246 ---

[[brainstorms]]
seq = 50
at_cycle = 197
trigger = "host-sdk theme completed, assess remaining epics and task priorities"
level = "quick"
key_insight = "host-sdk complete (5/5 tasks). public-api nearly done (3-4/4 criteria). real-std+gpu-error pending user close. Remaining work: codebase-health (workspace, build-auto, docs). Priority: docs.1+docs.2 (quick wins) → build-auto.1 → workspace.1 (biggest effort)."

[[brainstorms]]
seq = 51
at_cycle = 204
trigger = "All themes completed, all tasks done, no active work remains"
level = "quick"
key_insight = "All 5 epics at terminal state. real-std (5/5), gpu-error (4/4), public-api (4/4), codebase-health (4-5/5) — all pending user close. gpu-perf criteria 2-4 PARKED (out of scope). 203 tasks completed across 25+ themes. No new work to generate without user direction."

[[brainstorms]]
seq = 52
at_cycle = 205
trigger = "User: real-std NOT complete — ThreadLocal uses no_threads, all tests single-thread"
level = "standard"
key_insight = "real-std has 3 independent multi-thread blockers: (1) bump allocator static mut race, (2) errno static mut race, (3) std thread_local via no_threads = shared global. Plus HashMap panics (unsupported random). Plan: README fix → atomic alloc → errno → HashMap → TLS research."

[[brainstorms]]
seq = 53
at_cycle = 210
trigger = "All std-hardening themes completed, need new direction"
level = "standard"
key_insight = "3 epic proposals: (1) GPU concurrent data structures, (2) multi-kernel pipeline, (3) GPU debugging & observability. Chose #3 — lowest risk, highest novelty, improves dev experience. New epic: gpu-debug."

[[brainstorms]]
seq = 54
at_cycle = 214
trigger = "User reopened gpu-autonomous, requested deep brainstorm for v2 criteria"
level = "deep"
key_insight = "Skeptic challenged 3/4 original criteria: persistent kernel blocked by TDR, cross-launch state is trivial, GPU-driven workflow already done. Revised: multi-command kernel (TDR-safe), Pipeline API (adds real value), data-dependent iteration (genuinely new), hostcall session (centerpiece). 4 themes, 10 tasks."

[[brainstorms]]
seq = 55
at_cycle = 227
trigger = "gpu-autonomous v2 complete, gpu-debug criteria 3-4 remain"
level = "quick"
key_insight = "gpu-autonomous v2 all 4 themes done. gpu-debug has 2 remaining criteria: flight recorder (post-mortem trace dump) and conditional compilation (zero-overhead release builds). Both straightforward — no deep brainstorm needed."

[[brainstorms]]
seq = 56
at_cycle = 232
trigger = "User requested: align #[warp_async] with native async/await, including rustc modification"
level = "deep"
key_insight = "Initial analysis: WarpFuture and core::future::Future are fundamentally different execution models. Superseded by BS57 which reframed the problem."

# --- archived at cycle 266 ---

[[brainstorms]]
seq = 66
at_cycle = 257
trigger = "gpu-test-harness theme completed — no pending tasks"
level = "quick"
new_tasks_spawned = ["println-buffer.1"]
new_themes_spawned = ["println-buffer"]
themes_parked = []
themes_completed = ["gpu-test-harness"]
key_insight = "Integration test harness done. Next: integrate print_buffer with gpu-libc write() so println!() auto-buffers — highest-impact remaining optimization. Bridges printf-batch infra with real-std println!()."

# --- Archived at cycle 293 (keeping bs81-83 in state.toml) ---

[[brainstorms]]
seq = 80
at_cycle = 289
trigger = "all tasks done — quick brainstorm for next direction"
level = "quick"
new_tasks_spawned = ["release-prep.1", "release-prep.2", "showcase-demo.1", "showcase-demo.2"]
new_themes_spawned = ["release-prep", "showcase-demo"]
themes_completed = ["rustdoc-coverage", "arch-doc"]
key_insight = "Documentation phase complete. Both evergreen epics satisfied. Next: release-prep (v0.1.0 tag) then showcase-demo (parallel file search — plays to GPU strengths)."

[[brainstorms]]
seq = 79
at_cycle = 287
trigger = "all tasks done, no pending tasks — standard brainstorm for maintenance direction"
level = "standard"
new_tasks_spawned = ["rustdoc-coverage.1", "rustdoc-coverage.2", "arch-doc.1", "arch-doc.2"]
new_themes_spawned = ["rustdoc-coverage", "arch-doc"]
themes_completed = ["async-bulk-io", "build-fix"]
key_insight = "Both evergreen epics have remaining gaps: public-api needs rustdoc coverage for gpu-host, codebase-health README had stale .ps1 references (fixed inline)."

[[brainstorms]]
seq = 78
at_cycle = 284
trigger = "all themes completed, no pending tasks — deep brainstorm for new direction"
level = "deep"
new_tasks_spawned = ["async-bulk-io.1", "async-bulk-io.2", "async-bulk-io.3", "build-fix.1", "build-fix.2"]
new_themes_spawned = ["async-bulk-io", "build-fix"]
themes_completed = ["async-std-demo"]
key_insight = "Project graduated from 'can we do it?' to 'is it useful?'. Biggest gap is visibility, not features."

[[brainstorms]]
seq = 77
at_cycle = 281
trigger = "std-sysroot epic completed + std-sysroot-build theme completed — reassess async-std path"
level = "standard"
new_tasks_spawned = ["async-std-demo.1", "async-std-demo.2", "async-std-demo.3"]
new_themes_spawned = ["async-std-demo"]
themes_completed = ["std-sysroot-build", "ptx-codegen-fix"]
key_insight = "std-sysroot epic ALL 4 criteria met. ptx-codegen-fix closed. async-std remaining: C2+C3 demo."

[[brainstorms]]
seq = 76
at_cycle = 275
trigger = "user direction pivot — 5 priorities after async-pipeline demo success"
level = "standard"
new_tasks_spawned = ["std-sysroot-build.1", "std-sysroot-build.2", "std-sysroot-build.3", "std-sysroot-build.4", "gpu-executor.1", "gpu-executor.2", "ptx-codegen-fix.1", "ptx-codegen-fix.2"]
new_themes_spawned = ["std-sysroot-build", "gpu-executor", "ptx-codegen-fix", "warp-batch-io"]
themes_parked = ["warp-batch-io"]
themes_completed = ["async-yield"]
key_insight = "5 directions in priority order: (1) patched std in sysroot, (2) GPU executor, (3) PTX codegen fixes, (4) warp-batched hostcall (parked), (5) public API."

[[brainstorms]]
seq = 75
at_cycle = 274
trigger = "toolchain-auto theme completed — assess remaining work"
level = "quick"
new_tasks_spawned = []
new_themes_spawned = []
themes_completed = ["toolchain-auto"]
key_insight = "async-std epic 4/6 criteria met. Criterion 3 blocked on patched toolchain build."

[[brainstorms]]
seq = 74
at_cycle = 271
trigger = "user direction pivot — async yield not warp batching"
level = "quick"
new_tasks_spawned = ["async-yield.1", "async-yield.2", "async-yield.3"]
new_themes_spawned = ["async-yield"]
themes_completed = ["codebase-restructure"]
key_insight = "Real value: I/O yields warp to scheduler so other warps run compute."

# --- Archived at cycle 303 ---

[[brainstorms]]
seq = 83
at_cycle = 292
trigger = "all bs82 tasks done — quick brainstorm for remaining polish"
level = "quick"
new_tasks_spawned = ["readme-polish.1", "readme-polish.2", "contributing-guide.1"]
new_themes_spawned = ["readme-polish", "contributing-guide"]
themes_parked = []
themes_completed = ["ci-hygiene", "build-scripts", "example-docs"]
key_insight = "Last polish items: CI badge, warp-cooperative README/listing, CONTRIBUTING.md. test-expansion deferred (needs GPU hw). safety-docs deferred (large effort)."

[[brainstorms]]
seq = 82
at_cycle = 291
trigger = "all themes completed — standard brainstorm for evergreen epic refresh"
level = "standard"
new_tasks_spawned = ["ci-hygiene.1", "ci-hygiene.2", "ci-hygiene.3", "build-scripts.1", "example-docs.1", "example-docs.2"]
new_themes_spawned = ["ci-hygiene", "build-scripts", "example-docs"]
themes_parked = []
themes_completed = ["ci-update", "blog-draft"]
key_insight = "Project graduated to polish phase. 8 concrete gaps found: CI holes, missing run scripts, warp-cooperative cleanup, build script dirt, no per-example READMEs. No new epic needed."

# --- Archived at cycle 311 ---

[[brainstorms]]
seq = 86
at_cycle = 299
trigger = "all bs85 tasks completed — standard brainstorm for next direction"
level = "standard"
new_tasks_spawned = ["safety-audit.1", "safety-audit.2", "api-docs.1"]
new_themes_spawned = ["safety-audit", "api-docs"]
themes_parked = []
themes_completed = ["blog-update", "changelog-update"]
key_insight = "Project at natural plateau. Safety audit is highest-value remaining work (202 unsafe, only 10 with SAFETY). Updated public-api epic criteria to match actual API names."

# --- archived at cycle 327 ---

[[brainstorms]]
seq = 88
at_cycle = 309
trigger = "formal-verification epic completed, all bs87 tasks done"
level = "quick"
new_tasks_spawned = ["readme-tla.1", "executor-design.1"]
new_themes_spawned = ["readme-tla", "executor-design"]
key_insight = "formal-verification done (12th epic completed). README needs TLA+ section. GPU-side executor design doc is highest-value architectural work remaining."

[[brainstorms]]
seq = 87
at_cycle = 303
trigger = "all bs86 tasks completed, no ready tasks — deep brainstorm for new direction"
level = "deep"
new_tasks_spawned = ["tla-model.1", "tla-model.2", "tla-model.3", "build-stabilize.1", "validation-prep.1"]
new_themes_spawned = ["tla-model", "build-stabilize", "validation-prep"]
new_epics_spawned = ["formal-verification"]
key_insight = "Project needs depth, not breadth. TLA+ formal verification of CAS protocol is highest-value work without GPU."

# --- Archived at cycle 337 ---

[[brainstorms]]
seq = 90
at_cycle = 323
trigger = "executor-impl.4 done, gpu-executor epic completed, no ready tasks"
level = "quick"
new_tasks_spawned = ["bench-suite.4", "api-examples.1", "api-examples.2", "api-examples.3", "api-build.1"]
new_themes_spawned = ["api-examples", "api-build"]
new_epics_spawned = []
themes_parked = []
themes_completed = ["executor-impl", "std-sync"]
key_insight = "gpu-executor + extended-std epics done. Remaining: benchmarks (need analysis doc), public-api (need examples + build script). 5 new tasks across 3 themes."

[[brainstorms]]
seq = 89
at_cycle = 311
trigger = "user directed 4 new development areas: gpu-executor, extended-std, public-api, benchmarks"
level = "quick"
new_tasks_spawned = ["executor-impl.1", "executor-impl.2", "executor-impl.3", "executor-impl.4", "std-sync.1", "std-sync.2", "std-sync.3", "api-ergonomics.1", "api-ergonomics.2", "api-ergonomics.3", "bench-suite.1", "bench-suite.2", "bench-suite.3"]
new_themes_spawned = ["executor-impl", "std-sync", "api-ergonomics", "bench-suite"]
new_epics_spawned = ["gpu-executor", "extended-std", "benchmarks"]
themes_parked = []
themes_completed = []
key_insight = "User directed 4 areas: GPU executor impl (design exists), extended std (sync+collections), public API polish, benchmarks. 3 new epics + public-api upgraded to high priority. 13 tasks across 4 themes."

# --- Archived at cycle 342 ---

[[brainstorms]]
seq = 91
at_cycle = 326
trigger = "3 epics closed (gpu-executor, benchmarks, public-api), no high-priority active epics"
level = "standard"
new_tasks_spawned = ["channel-oneshot.1", "channel-oneshot.2", "channel-oneshot.3", "channel-mpsc.1", "channel-mpsc.2", "channel-waker.1", "channel-waker.2", "channel-demo.1"]
new_themes_spawned = ["channel-oneshot", "channel-mpsc", "channel-waker", "channel-demo"]
new_epics_spawned = ["gpu-channels"]
themes_parked = []
themes_completed = ["api-ergonomics"]
key_insight = "GPU executor works but tasks can't communicate. Channels (oneshot + mpsc) are the missing piece for real async patterns. Created gpu-channels epic with 4 themes, 8 tasks."

# --- Archived at cycle 349 ---

[[brainstorms]]
seq = 92
at_cycle = 328
trigger = "user pivot: demos are all I/O, need compute. Expose internal compute functions + build compelling demo."
level = "quick"
new_tasks_spawned = ["compute-audit.1", "compute-audit.2", "compute-extract.1", "compute-extract.2", "compute-extract.3", "demo-pipeline.1", "demo-pipeline.2", "demo-pipeline.3"]
new_themes_spawned = ["compute-audit", "compute-extract", "demo-pipeline"]
new_epics_spawned = ["compute-utils", "compute-demo"]
themes_parked = ["channel-mpsc", "channel-waker", "channel-demo"]
themes_completed = []
key_insight = "User pivot: all demos are I/O, no compute. We have GEMM, transformer ops, reductions buried in kernel code but not exposed. Two new epics: extract compute utils as public API, build compelling compute demo."

# --- Archived at cycle 365 ---

[[brainstorms]]
seq = 95
at_cycle = 345
trigger = "all 4 production-ready investigations complete, gpu-alloc already satisfied"
level = "quick"
key_insight = "gpu-alloc criterion already met by patched-std slab+bitmap allocator."

[[brainstorms]]
seq = 96
at_cycle = 349
trigger = "production-ready epic fully completed, no active high-priority epics"
level = "deep"
key_insight = "Project is technically feature-complete (v0.3 prototype). Highest-ROI next steps: warp-batch-io, ecosystem-launch, developer-experience."
