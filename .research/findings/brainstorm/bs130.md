# Brainstorm 130 — Activate transparent-data + auto-tuning

**Trigger**: gpu-dyn-dispatch story #60 completed. No active stories remain.
**Cycle**: 642 | **Level**: Standard (1 subagent)

---

## Dependency Check
- transparent-data (medium, invisible-exec): depends on unified-runtime ✅ → **READY**
- auto-tuning (low, perf-transparent): depends on compile-time-cost ✅ → **READY**
- conv-perf (medium): no deps but narrow (Winograd only)
- whole-program-partition (medium): depends on auto-parallel ❌ → BLOCKED
- ownership-memory, std-completeness, formal-verification, auto-parallel: low priority

## Activation

**Primary: transparent-data** — Most impactful remaining story. Every GPU program
currently uses explicit GpuVec; this makes `Vec<f32>` just work on GPU.

**Secondary: auto-tuning** — compile-time-cost just shipped; natural follow-on.
Applies to ALL kernels (broader than conv-perf's Winograd focus).

## Features & Tasks

**td-design** — Design transparent Vec<T> with residency tracking
- td-design.1: Investigation — audit GpuVec API, map transfer points
- td-design.2: Design — define TransparentVec<T> with Deref<Target=[T]>

**td-runtime** — Runtime residency tracking and automatic transfers
- td-runtime.1: Experiment — implement lazy host↔device transfer
- td-runtime.2: Experiment — integrate with unified-runtime kernel launch

**at-framework** — Auto-tuning for kernel parameter selection
- at-framework.1: Investigation — survey tunable params (block size, grid dim)
- at-framework.2: Experiment — warmup-based parameter search with caching

```toml
[[brainstorms]]
seq = 130
at_cycle = 642
trigger = "gpu-dyn-dispatch story #60 completed. No active stories remain."
level = "standard"
new_tasks_spawned = ["td-design.1", "td-design.2", "td-runtime.1", "td-runtime.2", "at-framework.1", "at-framework.2"]
new_features_spawned = ["td-design", "td-runtime", "at-framework"]
stories_completed = ["gpu-dyn-dispatch"]
key_insight = "transparent-data (Vec<f32> replaces GpuVec) and auto-tuning (runtime selects optimal kernel params) are the two highest-impact stories. Activating both advances invisible-exec and perf-transparent in parallel."
```
