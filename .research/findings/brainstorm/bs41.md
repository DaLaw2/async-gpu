# BS41 Synthesis: Break the PyTorch Dependency — Self-Diagnose Now

## Trigger
User demanded autonomous iteration. Generation produces garbage text.
3 consecutive brainstorms recommended "get PyTorch reference" — unacted.

## Key Consensus
1. Model limitation probability is <5% (skeptic wins). Top-5 being function words
   (" a", " the", " not") is pathological, not a knowledge gap.
2. Weight transpose is the #1 suspect (35%+). GPT-2 Conv1D stores [in, out],
   not nn.Linear's [out, in]. Common reimplementation bug.
3. We can diagnose WITHOUT PyTorch — internal consistency checks are sufficient.
4. NaN and wrong predictions may share a root cause (broken attention or weights).

## Key Disagreement Resolved
- Proposer: wait for PyTorch reference → rejected (user said stop asking)
- Skeptic: test internally NOW → accepted

## Action Plan (all autonomous, no user action)

### Task 1: full-inference.7 — WebFetch GPT-2 reference (10 min)
Query public GPT-2 endpoints to confirm whether GPT-2 predicts "Paris".
Epic: gpu-inference

### Task 2: full-inference.8 — Weight transpose verification (30 min)
Load c_attn.weight, compute input @ weight vs input @ weight.T on CPU,
compare against GPU GEMM output. Epic: gpu-inference

### Task 3: full-inference.9 — Per-layer intermediate predictions (15 min)
Apply LM head after each of 12 layers. Track where semantics are lost.
Epic: gpu-inference

### Task 4: full-inference.10 — LM head position audit (5 min)
Check predictions from last_pos-1, last_pos, last_pos+1.
Epic: gpu-inference

### Task 5: host-sdk.3 — Standalone example (later, lower priority)
Epic: public-api
