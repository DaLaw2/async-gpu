# Brainstorm 79 — Standard: Post-Completion Maintenance + Visibility
**Cycle**: 287 | **Date**: 2026-03-15 | **Level**: Standard (1-agent)

## Active Epics Assessment

### codebase-health (evergreen)

| # | Criterion | Status | Notes |
|---|-----------|--------|-------|
| 1 | Host crates share root workspace; GPU crates use separate build | **MET** | Root workspace + per-crate builds for nvptx64 |
| 2 | Nightly toolchain version defined in one place, CI + scripts derive from it | **MET** | `rust-toolchain.toml` is the single source (`nightly-2026-03-11`). Grep confirms no hardcoded nightly dates in any `.sh`/`.bat`/`.ps1` scripts. |
| 3 | README.md accurately reflects current project state | **PARTIAL** | README references `build-toolchain.ps1` (lines 88, 284) but that file does not exist — only `.bat` exists. The crate map says `scripts/` contains `.sh/.ps1` but should say `.sh/.bat`. Otherwise comprehensive and accurate. |
| 4 | Routine maintenance automated or self-documenting | **MET** | `ci-lint.sh`, `build-toolchain.sh`/`.bat`, `pre-push.sh`, patch scripts all exist |
| 5 | Research workflow handles codebase-health tasks | **MET** | 287 cycles, `/research` loop well-established |

**Remaining gap**: README has stale `.ps1` references; should say `.bat` for Windows.

### public-api (evergreen)

| # | Criterion | Status | Notes |
|---|-----------|--------|-------|
| 1 | gpu-host is a library crate with documented public API | **MOSTLY MET** | `#![warn(missing_docs)]` is set. 24 doc comments in lib.rs, 101 `pub` items across 9 source files. Module-level docs exist. Unknown: whether `cargo doc` builds cleanly without warnings. |
| 2 | Standalone example uses gpu-host as dependency | **MET** | 4 examples (hello-gpu, async-pipeline, async-io, vector-math) + warp-cooperative |
| 3 | Build system automated — single cargo command | **MET** | `build.rs` in each example handles PTX compilation |
| 4 | At least 3 working examples with documentation | **MET** | 4 documented examples in README with usage commands |

**Remaining gap**: Rustdoc coverage unknown — may have undocumented `pub` items triggering warnings.

## Gaps and Opportunities

### Gap 1: README references non-existent `build-toolchain.ps1`
- **Impact**: Low but embarrassing for first-time visitors
- **Fix**: Change `.ps1` references to `.bat` in README (2-line edit)
- **Effort**: 5 minutes

### Gap 2: `warp-cooperative` example has stale build artifacts
- `rustc-ice-*.txt` and `mir_dump/` directory are present in the example
- These should be gitignored or cleaned
- **Effort**: 10 minutes

### Gap 3: Rustdoc coverage for gpu-host
- `#![warn(missing_docs)]` is enabled but we don't know if `cargo doc` passes cleanly
- 101 pub items vs 24 doc comments suggests many undocumented items (especially in hostcall.rs with 39 pub items)
- **Effort**: 1-2 hours to audit and add doc comments

### Gap 4: No architecture document
- README is excellent but dense — a separate `ARCHITECTURE.md` would help contributors
- bs78 identified this as valuable work
- **Effort**: 1-2 hours

### Gap 5: Project visibility (blog post / publication)
- bs78 consensus: "biggest risk is continuing to build features for an audience of zero"
- A blog post or technical writeup explaining the approach would be highest-ROI external work
- This is **outside the agent's scope** — requires user action (writing, publishing)
- **Recommendation**: Agent can draft technical content; user publishes

### Gap 6: No CHANGELOG or release tagging
- 287 research cycles but no versioned releases
- A `CHANGELOG.md` or git tags would help external users understand project maturity
- **Effort**: 30 minutes for initial version

## Recommendations

### Theme: readme-script-fix (codebase-health)
Fix the `.ps1` → `.bat` references in README. Trivial but important for first impressions.

**Tasks**:
- `readme-script-fix.1` — Fix README references to `build-toolchain.ps1` → `build-toolchain.bat`, update crate map scripts line

### Theme: rustdoc-coverage (public-api)
Audit and improve rustdoc coverage for gpu-host public API.

**Tasks**:
- `rustdoc-coverage.1` — Investigation: run `cargo doc` on gpu-host, catalog all missing-doc warnings
- `rustdoc-coverage.2` — Experiment: add doc comments to all undocumented `pub` items in gpu-host

### Theme: example-cleanup (codebase-health)
Clean stale artifacts from warp-cooperative example.

**Tasks**:
- `example-cleanup.1` — Remove `rustc-ice-*.txt`, `mir_dump/` from warp-cooperative example; update `.gitignore`

### Theme: arch-doc (public-api)
Write `ARCHITECTURE.md` covering the system design: hostcall protocol, warp-cooperative async, PAL layer, crate relationships.

**Tasks**:
- `arch-doc.1` — Design: outline ARCHITECTURE.md structure (hostcall protocol, warp-cooperative MIR pass, PAL, crate map)
- `arch-doc.2` — Write ARCHITECTURE.md

### Theme: release-prep (codebase-health)
Tag a v0.1.0 release, write CHANGELOG.

**Tasks**:
- `release-prep.1` — Investigation: decide version numbering, catalog key milestones for CHANGELOG
- `release-prep.2` — Create CHANGELOG.md and git tag v0.1.0

## Priority Order

1. **readme-script-fix** (codebase-health) — 5 minutes, fixes a factual error visible to every visitor. Do first.
2. **example-cleanup** (codebase-health) — 10 minutes, removes embarrassing artifacts from repo.
3. **rustdoc-coverage** (public-api) — 1-2 hours, directly addresses the undocumented-API gap. Important for anyone trying to use gpu-host as a library.
4. **arch-doc** (public-api) — 1-2 hours, helps potential contributors understand the system. High value for visibility.
5. **release-prep** (codebase-health) — 30 minutes, signals project maturity. Lower priority since the project is research-stage.

**Rationale**: Items 1-2 are quick wins that improve first impressions. Item 3 directly serves the public-api epic's criterion 1. Item 4 is the highest-value documentation work identified in bs78. Item 5 is nice-to-have.

**Deferred**: Blog post / publication requires user action. Agent can draft content if requested but should not create marketing materials unprompted.
