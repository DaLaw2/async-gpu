# Autonomous Research Loop State File
# Three-level hierarchy: Epic → Theme → Task
# Epics are user-defined high-level goals; Themes are research directions; Tasks are actionable items within a theme.

[meta]
created = "2026-03-11"
total_cycles = 192     # Monotonic counter, incremented after each completed phase
completed_tasks = 191
status = "running"     # ready | running | blocked | completed

# Current cycle position within Think/Do/Check/Adapt
current_mode = "do"
current_step = "do.execute"
current_task_id = "std-migration.4"
last_summary = "std-fs.5 done: ErrorKind::NotFound + raw_os_error(2) verified. Display for io::Error crashes (known issue — error_string allocates). Next: std-migration.4 (stdin e2e), then host-sdk.2."

# Sequence counters for unique file naming across iterations
brainstorm_seq = 49
review_seq = 5

# Brainstorm trigger control (event-driven, no fixed interval)

# ============================================================
# Epics (active only — completed epics archived in archive/)
# ============================================================

[[epics]]
id = "gpu-perf"
title = "GPU Inference Performance Optimization"
status = "active"
priority = "high"
success_criteria = [
    "KV cache eliminates redundant recomputation — only new token passes through model per step",
    "PARKED: Per-token generation latency < 20ms (down from ~143ms)",
    "PARKED: BF16/TF32 MMA inference matches f32 FMA output (top-5 agreement for 3+ prompts)",
    "PARKED: Mixed precision pipeline: GEMM via Tensor Core, everything else f32",
]
# Criterion 1: DONE — KV cache verified (50/50 tokens match, 2.07x speedup, 68ms/tok)
# Criterion 2: PARKED — 68ms with KV cache, 35ms full forward; reaching 20ms requires
#   persistent kernels, fused ops, reduced sync overhead. Significant engineering investment
#   with uncertain ROI given single-SM workload and per-kernel launch overhead.
# Criterion 3: PARKED — f16, bf16, tf32 MMA all fail 12-layer inference (0/3 top-1 match).
#   Root cause: naive f32→reduced-precision truncation causes cascading error through residual
#   connections. Requires quantization-aware training or post-training calibration (PTQ) with
#   per-channel scaling — out of scope for this project.
# Criterion 4: PARKED — depends on criterion 3.

[[epics]]
id = "real-std"
title = "Real std on GPU — replace macros with actual Rust std"
status = "active"
priority = "high"
success_criteria = [
    "GPU kernel can `use std::io::Write` and call `println!()` without custom macros",
    "Vec, String, Box work on GPU via #[global_allocator] backed by hostcall-aware allocator",
    "std::fs::File::create() and write_all() work on GPU via hostcall",
    "std::io::stdin().read_line() works on GPU via hostcall",
    "PAL/libc facade routes std sys calls to hostcall framework",
]

[[epics]]
id = "gpu-error"
title = "GPU-side error handling — Result propagation from GPU to host"
status = "active"
priority = "medium"
success_criteria = [
    "GPU kernel can use `?` operator and return Result<T, E> to host",
    "Errors propagate across async boundaries (hostcall errors → Result, not panic)",
    "Host receives structured error info (error type + message), not just trap/exit code",
    "std::io and std::fs errors on GPU produce proper std::io::Error, not panic",
]

[[epics]]
id = "codebase-health"
title = "Codebase Health — build system, CI, docs, workflow automation"
status = "active"
priority = "medium"
success_criteria = [
    "Single workspace with all crates — `cargo build -p <any-crate>` works from root",
    "Nightly toolchain version defined in one place, CI + scripts derive from it",
    "README.md accurately reflects current project state, crate map, and setup instructions",
    "Routine maintenance tasks (new crate → CI, new PTX → stub list) are automated or self-documenting",
    "Research workflow handles codebase-health tasks (lint fix, doc, CI update) without full Think/Do/Check overhead",
]

[[epics]]
id = "public-api"
title = "Public API — Host SDK + Reusable GPU Kernel Library"
status = "active"
priority = "medium"
success_criteria = [
    "gpu-host is a library crate with documented public API (GpuDevice, KernelBuilder, HostcallRuntime)",
    "A standalone example program uses gpu-host as a dependency to run a GPU kernel",
    "Build system automated — single cargo command compiles host + kernel",
    "At least 3 working examples (hello-gpu, async-io, compute) with documentation",
]

# ============================================================
# Themes (active only — completed/parked archived in archive/)
# ============================================================

[[themes]]
id = "mma-fix"
title = "Fix Tensor Core MMA kernel for GPT-2 dimensions"
status = "completed"
spawned_by = "bs43"
epic = "gpu-perf"
goal = "Diagnose and fix full_gemm_f32in to produce correct results at 768x768+"
success_criteria = [
    "full_gemm_f32in matches gemm_f32 output for 768x768, 768x2304, 768x3072, 3072x768",
    "MMA-based inference produces same top-5 tokens as f32 FMA for 3+ prompts",
]
# Outcome: MMA kernel correct at all dimensions (criterion 1 met). Criterion 2 NOT met —
# f16 precision loss over 12 layers causes top-5 divergence. 1.8x speedup confirmed.
# Future: TF32/BF16 MMA variant needed for inference-quality precision.

[[themes]]
id = "kv-cache"
title = "KV cache for efficient autoregressive generation"
status = "completed"
spawned_by = "bs43"
epic = "gpu-perf"
goal = "Implement KV cache to eliminate redundant recomputation in generation loop"
success_criteria = [
    "Per-step forward pass only processes new token (not full sequence)",
    "KV-cached output matches non-cached output for 50+ tokens",
    "Per-token latency < 20ms",
]

[[themes]]
id = "repo-cleanup"
title = "Repository cleanup + example expansion"
status = "completed"
spawned_by = "bs43"
epic = "public-api"
goal = "Remove tracked build artifacts, eliminate duplicate files, add 2+ standalone examples"
success_criteria = [
    "No PTX files tracked in git",
    "No duplicate model files",
    "At least 3 working examples (including hello-gpu) with documentation",
]

[[themes]]
id = "mixed-precision"
title = "BF16/TF32 mixed precision Tensor Core inference"
status = "completed"
spawned_by = "bs45"
epic = "gpu-perf"
goal = "Implement BF16 or TF32 MMA GEMM that matches f32 FMA inference quality"
success_criteria = [
    "BF16 or TF32 MMA GEMM matches gemm_f32 within acceptable tolerance for all GPT-2 dims",
    "Mixed-precision inference produces same top-5 tokens as f32 FMA for 3+ prompts",
]
# Outcome: Criterion 1 MET (bf16 vs f16 max divergence 0.2042). Criterion 2 NOT MET —
# bf16 (7-bit mantissa) causes top-5 divergence over 12 layers, same as f16 (10-bit).
# BF16 is 30% faster than f32 FMA. TF32 (19-bit) would be needed for inference quality.

[[themes]]
id = "host-sdk"
title = "Extract reusable host-side SDK library from gpu-host"
status = "active"
spawned_by = "bs38"
epic = "public-api"
goal = "Extract a library crate from gpu-host that provides clean API for kernel management, hostcall runtime, and memory management"
success_criteria = [
    "gpu-host exports a library crate with public GpuDevice + KernelBuilder + HostcallRuntime types",
    "A standalone example can use gpu-host as a library dependency",
    "All existing tests continue to pass",
]

[[themes]]
id = "tf32-mma"
title = "TF32 Tensor Core MMA experiment"
status = "completed"
spawned_by = "bs47"
epic = "gpu-perf"
goal = "Test whether TF32 MMA (10-bit mantissa, f32 exponent) provides sufficient precision for 12-layer inference"
success_criteria = [
    "TF32 MMA GEMM matches gemm_f32 within acceptable tolerance for GPT-2 dims",
    "TF32 inference produces same top-5 tokens as f32 FMA for 3+ prompts (decision gate)",
]

[[themes]]
id = "gpu-error-propagation"
title = "GPU→host error propagation via output buffer convention"
status = "completed"
spawned_by = "bs48"
epic = "gpu-error"
goal = "Define and implement GPU→host error propagation using output buffer convention"
success_criteria = [
    "GpuKernelResult type defined with Ok/Err variants, encodable in 64 bytes",
    "Hostcall callers check CONTROL_ERROR and return Result instead of bool/panic",
    "Host SDK reads error buffer after kernel launch and returns Result<(), GpuKernelError>",
    "Panic handler writes to error buffer instead of (or before) trapping",
]

[[themes]]
id = "std-migration"
title = "Migrate hostcall/IO kernels to use patched std"
status = "completed"
spawned_by = "bs48"
epic = "real-std"
goal = "Create gpu-kernel-std crate using patched std while keeping compute kernels as no_std"
success_criteria = [
    "gpu-kernel-std builds with -Zbuild-std=std and can use println!(), Vec, format!()",
    "Hostcall protocol code shared (not duplicated) between std and no_std kernels",
    "At least one async pipeline kernel uses std types",
]

[[themes]]
id = "std-fs"
title = "std::fs on GPU via hostcall with proper error handling"
status = "completed"
spawned_by = "bs48"
epic = "real-std"
goal = "Implement std::fs on GPU via hostcall, with proper error handling"
success_criteria = [
    "std::fs::File::create() and write_all() work on GPU via hostcall",
    "File I/O errors produce proper std::io::Error (not panic)",
    "std::io::stdin().read_line() works (fix OnceLock in stdin path)",
]

# ============================================================
# Tasks (pending/active only — done tasks archived)
# ============================================================

# --- workspace-consolidation (codebase-health) ---

[[themes]]
id = "workspace-consolidation"
title = "Consolidate workspace, configs, and toolchain version"
status = "active"
spawned_by = "user"
epic = "codebase-health"
goal = "Single Cargo workspace, deduplicated .cargo/config.toml, single-source nightly version"
success_criteria = [
    "All crates buildable from workspace root via cargo build -p <name>",
    "No duplicate .cargo/config.toml across kernel crates",
    "Nightly version defined once, referenced by CI + scripts",
]

[[themes]]
id = "build-automation"
title = "Build system automation — PTX stubs, CI sync"
status = "active"
spawned_by = "user"
epic = "codebase-health"
goal = "Eliminate manual maintenance when adding crates or PTX files"
success_criteria = [
    "PTX stub list auto-derived, not manually maintained in 3 places",
    "CI workflow and local lint stay in sync automatically",
]

[[themes]]
id = "docs"
title = "Documentation — README, API docs, architecture"
status = "active"
spawned_by = "user"
epic = "codebase-health"
goal = "README reflects current state, public APIs documented"
success_criteria = [
    "README.md has accurate crate map, setup instructions, and example descriptions",
    "All public API crates have doc-comments on public items",
]

[[themes]]
id = "workflow-improvement"
title = "Research workflow improvements for routine tasks"
status = "completed"
spawned_by = "user"
epic = "codebase-health"
goal = "Lightweight handling for maintenance tasks that don't need full research loop"
success_criteria = [
    "Routine tasks (CI fix, fmt, doc) have a fast path that skips Think/Check",
    "New crate additions are detected and handled semi-automatically",
]
# Outcome: /maintain skill created as event-driven housekeeping.
# Invoked by /research at do.save, or standalone. No state.toml bloat.

# --- mma-fix (gpu-perf) ---

[[tasks]]
id = "mma-fix.1"
theme = "mma-fix"
title = "Diagnose MMA kernel failure at 768x768"
kind = "investigation"
status = "done"
depends_on = []
spawned_by = "bs43"
research_questions = [
    "Does full_gemm (f16x2 input) also fail at 768x768, or only full_gemm_f32in?",
    "Is the host-side grid_dim.y correctly set for large N?",
    "Does bar_sync() generate block-level or warp-level sync?",
    "What do shared memory contents look like for tile (0,0) vs (0,1)?",
]

[[tasks]]
id = "mma-fix.2"
theme = "mma-fix"
title = "Fix and validate MMA GEMM for all GPT-2 dimensions"
kind = "experiment"
status = "skipped"
depends_on = ["mma-fix.1"]
spawned_by = "bs43"
research_questions = [
    "Does the fix produce correct results for 768x768, 768x2304, 768x3072, 3072x768?",
    "Does the fix match gemm_f32 reference within atol=0.1 (f16 precision)?",
]

[[tasks]]
id = "mma-fix.3"
theme = "mma-fix"
title = "MMA inference end-to-end validation"
kind = "experiment"
status = "done"
depends_on = ["mma-fix.1"]
spawned_by = "bs43"
research_questions = [
    "Does MMA inference produce same top-5 tokens as f32 FMA for 3+ prompts?",
    "What is the per-token latency with MMA vs f32 FMA?",
]

# --- kv-cache (gpu-perf) ---

[[tasks]]
id = "kv-cache.1"
theme = "kv-cache"
title = "KV cache architecture and memory layout design"
kind = "design"
status = "done"
depends_on = []
spawned_by = "bs43"
research_questions = [
    "What is the optimal device-side buffer layout for 12-layer KV cache?",
    "How much GPU memory is needed for max_seq=128 vs 1024?",
    "What kernel changes are needed for cache-append and cached attention?",
]

[[tasks]]
id = "kv-cache.2"
theme = "kv-cache"
title = "Implement KV cache for single-layer attention"
kind = "experiment"
status = "done"
depends_on = ["kv-cache.1"]
spawned_by = "bs43"
research_questions = [
    "Does cached attention output match non-cached for a single layer?",
    "What is the latency improvement for single-layer cached vs non-cached?",
]

[[tasks]]
id = "kv-cache.3"
theme = "kv-cache"
title = "Full 12-layer KV-cached inference"
kind = "experiment"
status = "done"
depends_on = ["kv-cache.2"]
spawned_by = "bs43"
research_questions = [
    "Does 12-layer cached output match non-cached for 50+ tokens?",
    "What is the per-token latency with KV cache?",
]

# --- repo-cleanup (public-api) ---

[[tasks]]
id = "repo-cleanup.1"
theme = "repo-cleanup"
title = "Gitignore PTX + remove duplicate model file"
kind = "experiment"
status = "done"
depends_on = []
spawned_by = "bs43"
research_questions = [
    "Are any PTX files referenced by path in code that would break after git rm --cached?",
    "Which model path does inference code reference — root or crate-local?",
]

[[tasks]]
id = "repo-cleanup.2"
theme = "repo-cleanup"
title = "Extract async-io example from pipeline tests"
kind = "experiment"
status = "done"
depends_on = ["repo-cleanup.1"]
spawned_by = "bs43"
research_questions = [
    "Can the file transform pipeline become a standalone example using gpu-host public API?",
    "What minimal kernel subset is needed for the example?",
]

[[tasks]]
id = "repo-cleanup.3"
theme = "repo-cleanup"
title = "Extract compute example from search/GEMM tests"
kind = "experiment"
status = "done"
depends_on = ["repo-cleanup.1"]
spawned_by = "bs43"
research_questions = [
    "Can vector search or GEMM become a standalone example?",
    "What is the simplest compute-focused example that demonstrates GPU kernels?",
]

# --- mixed-precision (gpu-perf) ---

[[tasks]]
id = "mixed-precision.1"
theme = "mixed-precision"
title = "BF16 MMA GEMM kernel implementation"
kind = "experiment"
status = "done"
depends_on = []
spawned_by = "bs45"
research_questions = [
    "Does sm_86 support mma.sync with bf16 inputs?",
    "Does BF16 MMA GEMM match gemm_f32 within acceptable tolerance for GPT-2 dims?",
    "What is the speedup vs f32 FMA and vs f16 MMA?",
]

[[tasks]]
id = "mixed-precision.2"
theme = "mixed-precision"
title = "BF16 mixed-precision inference validation"
kind = "experiment"
status = "done"
depends_on = ["mixed-precision.1"]
spawned_by = "bs45"
research_questions = [
    "Does BF16 MMA inference produce same top-5 tokens as f32 FMA for 3+ prompts?",
    "What is the per-token latency with BF16 MMA vs f32 FMA?",
]

# --- host-sdk (public-api) ---

[[tasks]]
id = "host-sdk.3"
theme = "host-sdk"
title = "Standalone example using SDK as library dependency"
kind = "experiment"
status = "pending"
depends_on = ["host-sdk.2"]
spawned_by = "bs38"
research_questions = [
    "Can an external binary crate depend on gpu-host and launch a kernel?",
    "Does the example work from a clean workspace setup?",
]

# --- tf32-mma (gpu-perf) ---

[[tasks]]
id = "tf32-mma.1"
theme = "tf32-mma"
title = "TF32 MMA GEMM kernel (m16n8k8)"
kind = "experiment"
status = "done"
depends_on = []
spawned_by = "bs47"
research_questions = [
    "Does sm_86 support mma.sync with .tf32 qualifier?",
    "Does TF32 MMA GEMM match gemm_f32 within acceptable tolerance for GPT-2 dims?",
    "How does TF32 precision compare to bf16 and f16 MMA?",
]

[[tasks]]
id = "tf32-mma.2"
theme = "tf32-mma"
title = "TF32 inference validation (decision gate)"
kind = "experiment"
status = "done"
depends_on = ["tf32-mma.1"]
spawned_by = "bs47"
research_questions = [
    "Does TF32 MMA inference produce same top-5 as f32 FMA for 3+ prompts?",
    "Decision gate: if fails, park gpu-perf criteria 3+4",
]

# --- host-sdk (public-api) ---

[[tasks]]
id = "host-sdk.2"
theme = "host-sdk"
title = "Extract reusable API types from gpu-host"
kind = "design"
status = "pending"
depends_on = []
spawned_by = "bs47"
research_questions = [
    "What types should be public: GpuDevice, KernelBuilder, HostcallRuntime?",
    "Can gpu-host be both a lib crate and a bin crate?",
    "What is the minimal public API surface?",
]

[[tasks]]
id = "host-sdk.4"
theme = "host-sdk"
title = "Two additional examples (async-io, compute) with documentation"
kind = "experiment"
status = "pending"
depends_on = ["host-sdk.2"]
spawned_by = "bs47"
research_questions = [
    "What is the simplest async-io example demonstrating GPU file access?",
    "What is the simplest compute example demonstrating GPU kernel execution?",
]

[[tasks]]
id = "host-sdk.5"
theme = "host-sdk"
title = "Automate build system — single cargo command"
kind = "experiment"
status = "pending"
depends_on = ["host-sdk.2"]
spawned_by = "bs47"
research_questions = [
    "Can a build.rs compile the kernel PTX automatically?",
    "How to handle the nightly + nvptx64 toolchain requirement?",
]

# --- gpu-error-propagation (gpu-error) ---

[[tasks]]
id = "gpu-error-propagation.1"
theme = "gpu-error-propagation"
title = "Design GpuKernelResult ABI + error buffer layout"
kind = "design"
status = "done"
depends_on = []
spawned_by = "bs48"
research_questions = [
    "What layout for GpuKernelResult: tag + category + errno + message? How many bytes?",
    "Should error buffer be a kernel parameter or embedded in hostcall buffer?",
    "How does ? operator desugar: write to buffer + early return?",
]

[[tasks]]
id = "gpu-error-propagation.2"
theme = "gpu-error-propagation"
title = "Implement GPU-side error checking in hostcall helpers"
kind = "experiment"
status = "done"
depends_on = ["gpu-error-propagation.1"]
spawned_by = "bs48"
research_questions = [
    "Can hostcall helpers (gpu_hostcall_print, file ops) return Result instead of bool?",
    "Does checking CONTROL_ERROR add measurable overhead?",
]

[[tasks]]
id = "gpu-error-propagation.3"
theme = "gpu-error-propagation"
title = "Implement host-side error extraction from kernel result buffer"
kind = "experiment"
status = "done"
depends_on = ["gpu-error-propagation.1"]
spawned_by = "bs48"
research_questions = [
    "How does host read GpuKernelResult after kernel launch?",
    "Can KernelBuilder::launch() return Result<(), GpuKernelError>?",
]

[[tasks]]
id = "gpu-error-propagation.4"
theme = "gpu-error-propagation"
title = "Non-destructive panic handler (error buffer + cooperative exit)"
kind = "experiment"
status = "done"
depends_on = ["gpu-error-propagation.1"]
spawned_by = "bs48"
research_questions = [
    "Can panic handler write to error buffer and return instead of trap?",
    "Does cooperative exit (no trap) preserve CUDA context for recovery?",
    "How to propagate panic info without killing the entire kernel launch?",
]

# --- std-migration (real-std) ---

[[tasks]]
id = "std-migration.1"
theme = "std-migration"
title = "Create gpu-kernel-std crate + verify println/Vec/format work"
kind = "experiment"
status = "done"
depends_on = []
spawned_by = "bs48"
research_questions = [
    "Can a new crate build with -Zbuild-std=std and produce valid PTX?",
    "Do println!, Vec, String, format! all work without duplicating hostcall code?",
    "What is the PTX size difference vs no_std kernel?",
]

[[tasks]]
id = "std-migration.2"
theme = "std-migration"
title = "Factor shared hostcall protocol into std-compatible library"
kind = "design"
status = "done"
depends_on = ["std-migration.1"]
spawned_by = "bs48"
research_questions = [
    "Can gpu-runtime be made std-compatible (remove no_std requirement)?",
    "What is the minimal shared interface between std and no_std kernel crates?",
]

[[tasks]]
id = "std-migration.3"
theme = "std-migration"
title = "Port async pipeline kernels to gpu-kernel-std with Result types"
kind = "experiment"
status = "done"
depends_on = ["std-migration.2", "gpu-error-propagation.2"]
spawned_by = "bs48"
research_questions = [
    "Can async pipeline kernels use std::io + Result error handling?",
    "Does the combined std + error propagation work end-to-end?",
]

# --- std-fs (real-std + gpu-error) ---

[[tasks]]
id = "std-fs.1"
theme = "std-fs"
title = "Add SERVICE_FS_* hostcall services + host-side handlers"
kind = "experiment"
status = "done"
depends_on = []
spawned_by = "bs48"
research_questions = [
    "What fs services are needed: open, read, write, close, stat?",
    "Can existing SERVICE_OPEN/WRITE/READ/CLOSE be reused or need new opcodes?",
]

[[tasks]]
id = "std-fs.2"
theme = "std-fs"
title = "Implement gpu-libc open/read/write/close via hostcall (replace stubs)"
kind = "experiment"
status = "done"
depends_on = ["std-fs.1"]
spawned_by = "bs48"
research_questions = [
    "Can gpu-libc stubs call hostcall protocol without depending on gpu-runtime?",
    "How does gpu-libc get access to the hostcall buffer pointer?",
]

[[tasks]]
id = "std-fs.3"
theme = "std-fs"
title = "Fix OnceLock for stdin path (spinlock replacement)"
kind = "experiment"
status = "done"
depends_on = ["std-migration.1"]
spawned_by = "bs48"
research_questions = [
    "Can OnceLock use GPU atomic spinlock instead of futex?",
    "Does fixing OnceLock unblock std::io::stdin().read_line()?",
]

[[tasks]]
id = "std-fs.4"
theme = "std-fs"
title = "End-to-end: File::create + write_all + read with error handling"
kind = "experiment"
status = "done"
depends_on = ["std-fs.2", "std-fs.3", "gpu-error-propagation.2"]
spawned_by = "bs48"
research_questions = [
    "Does std::fs::File::create() work on GPU via hostcall?",
    "Do file I/O errors produce proper std::io::Error?",
]

# --- real-std verification ---

[[tasks]]
id = "std-migration.4"
theme = "std-migration"
title = "Verify stdin().read_line() end-to-end on GPU"
kind = "experiment"
status = "active"
depends_on = ["std-fs.3"]
spawned_by = "bs49"
research_questions = [
    "Does std::io::stdin().read_line() work via hostcall STDIN?",
    "Does gpu_stdin_read need to be wired to SERVICE_STDIN?",
]

# --- workspace-consolidation (codebase-health) ---

[[tasks]]
id = "workspace.1"
theme = "workspace-consolidation"
title = "Consolidate all crates into single Cargo workspace"
kind = "experiment"
status = "pending"
depends_on = []
spawned_by = "user"
research_questions = [
    "Can all 14 crates coexist in one workspace despite mixed stable/nightly targets?",
    "How to handle GPU kernel crates (nvptx64) vs host crates (x86) in one workspace?",
    "What resolver settings are needed?",
]

[[tasks]]
id = "workspace.2"
theme = "workspace-consolidation"
title = "Deduplicate .cargo/config.toml across kernel crates"
kind = "experiment"
status = "pending"
depends_on = ["workspace.1"]
spawned_by = "user"
research_questions = [
    "Can kernel crates inherit target config from workspace root?",
    "Does per-crate override still work if needed?",
]

[[tasks]]
id = "workspace.3"
theme = "workspace-consolidation"
title = "Single-source nightly toolchain version"
kind = "experiment"
status = "pending"
depends_on = ["workspace.1"]
spawned_by = "user"
research_questions = [
    "Can ci-lint.sh read NIGHTLY from rust-toolchain.toml instead of hardcoding?",
    "Can CI workflow reference the same file?",
]

# --- build-automation (codebase-health) ---

[[tasks]]
id = "build-auto.1"
theme = "build-automation"
title = "Auto-generate PTX stub list from crate scan"
kind = "experiment"
status = "pending"
depends_on = []
spawned_by = "user"
research_questions = [
    "Can ci-lint.sh discover PTX filenames from gpu-host include_str! calls?",
    "Can CI workflow share the same logic?",
]

[[tasks]]
id = "build-auto.2"
theme = "build-automation"
title = "Single-source CI: generate build.yml from ci-lint.sh or shared config"
kind = "design"
status = "pending"
depends_on = ["build-auto.1"]
spawned_by = "user"
research_questions = [
    "What is the best way to keep CI and local lint in sync?",
    "Should ci-lint.sh be the source of truth, or a shared crate list?",
]

# --- docs (codebase-health) ---

[[tasks]]
id = "docs.1"
theme = "docs"
title = "Rewrite README.md to reflect current project state"
kind = "experiment"
status = "pending"
depends_on = []
spawned_by = "user"
research_questions = [
    "What sections are outdated or missing?",
    "Should README include crate dependency diagram?",
    "What setup instructions are needed for new contributors?",
]

[[tasks]]
id = "docs.2"
theme = "docs"
title = "Add doc-comments to undocumented public APIs"
kind = "experiment"
status = "pending"
depends_on = []
spawned_by = "user"
research_questions = [
    "Which crates have missing public API docs (gpu-critical-section, gpu-libc)?",
    "Should -D missing_docs be enforced on more crates?",
]

# --- workflow-improvement (codebase-health) ---

[[tasks]]
id = "workflow.1"
theme = "workflow-improvement"
title = "/maintain skill created — event-driven housekeeping"
kind = "design"
status = "done"
depends_on = []
spawned_by = "user"
research_questions = [
    "What tasks are routine (CI fix, doc update, fmt fix) vs research?",
    "Should routine tasks skip Think/Check phases entirely?",
]

# --- gpu-error verification ---

[[tasks]]
id = "std-fs.5"
theme = "std-fs"
title = "Verify std::io::Error mapping for file-not-found and permission errors"
kind = "experiment"
status = "done"
depends_on = ["std-fs.4"]
spawned_by = "bs49"
research_questions = [
    "Does File::open on nonexistent file produce ErrorKind::NotFound?",
    "Does the error message contain the errno description?",
]

# ============================================================
# Brainstorms (last 3 only — older brainstorms archived)
# ============================================================

[[brainstorms]]
seq = 49
at_cycle = 191
trigger = "Two themes completed (std-fs, std-migration), assess remaining epic criteria"
level = "quick"
new_tasks_spawned = ["std-migration.4", "std-fs.5"]
new_themes_spawned = []
themes_parked = []
themes_completed = ["std-fs", "std-migration"]
key_insight = "real-std 4/5 criteria met, gpu-error 3/4 met. Two quick verification tasks: stdin e2e test + error mapping test. After those, both epics should be closeable. host-sdk still needs full attention (5 pending tasks)."

[[brainstorms]]
seq = 48
at_cycle = 180
trigger = "Two new epics (real-std, gpu-error) with no themes or tasks"
level = "standard"
new_tasks_spawned = ["gpu-error-propagation.1", "gpu-error-propagation.2", "gpu-error-propagation.3", "gpu-error-propagation.4", "std-migration.1", "std-migration.2", "std-migration.3", "std-fs.1", "std-fs.2", "std-fs.3", "std-fs.4"]
new_themes_spawned = ["gpu-error-propagation", "std-migration", "std-fs"]
themes_parked = []
themes_completed = []
key_insight = "std-build-test already proves patched std works. Gap: gpu-kernel doesn't use it, gpu-libc stubs return ENOSYS, error handling nonexistent. Strategy: error propagation first (unblocks Result in std), then selective std migration (new crate, not all 65+ kernels), then fs via gpu-libc hostcall."

[[brainstorms]]
seq = 47
at_cycle = 178
trigger = "All themes completed/parked, no active tasks in either epic"
level = "standard"
new_tasks_spawned = ["tf32-mma.1", "tf32-mma.2", "host-sdk.2", "host-sdk.4", "host-sdk.5"]
new_themes_spawned = ["tf32-mma"]
themes_parked = []
themes_completed = ["mixed-precision"]
key_insight = "mixed-precision complete (1/2 criteria). bf16/f16 both fail 12-layer inference. TF32 (10-bit mantissa like f16) is bounded experiment — decision gate for gpu-perf criteria 3+4. Un-parked host-sdk for public-api (user wants reusable utilities)."

[[brainstorms]]
seq = 46
at_cycle = 177
trigger = "Brainstorm interval (3 tasks completed since bs45)"
level = "quick"
new_tasks_spawned = []
new_themes_spawned = []
themes_parked = []
themes_completed = []
key_insight = "mixed-precision.1 done: BF16≈F16 precision (max divergence 0.2042). Proceed with mixed-precision.2 (inference validation). Expect same outcome as f16 MMA inference (top-5 divergence). public-api epic needs attention after mixed-precision completes."

[[brainstorms]]
seq = 45
at_cycle = 174
trigger = "User request: add mixed-precision/BF16 to gpu-perf epic, prioritize KV cache"
level = "quick"
new_tasks_spawned = ["mixed-precision.1", "mixed-precision.2"]
new_themes_spawned = ["mixed-precision"]
themes_parked = ["host-sdk"]
themes_completed = []
key_insight = "Added mixed-precision theme (BF16 MMA) to gpu-perf. Parked host-sdk (host-sdk.3 blocked by missing host-sdk.2). Priority: kv-cache.2 first, then mixed-precision.1."

[[brainstorms]]
seq = 44
at_cycle = 171
trigger = "Brainstorm interval (3 tasks completed since bs43)"
level = "quick"
new_tasks_spawned = []
new_themes_spawned = []
themes_parked = []
themes_completed = ["mma-fix"]
key_insight = "MMA kernel correct but f16 insufficient for 12-layer inference (top-5 diverges). 1.8x speed gain confirmed. mma-fix theme completed. Next priority: KV cache (gpu-perf) and examples (public-api) in parallel."

[[brainstorms]]
seq = 43
at_cycle = 169
trigger = "New gpu-perf epic + user requests cleanup/examples + Product Ready"
level = "standard"
new_tasks_spawned = ["mma-fix.1", "mma-fix.2", "mma-fix.3", "kv-cache.1", "kv-cache.2", "kv-cache.3", "repo-cleanup.1", "repo-cleanup.2", "repo-cleanup.3"]
new_themes_spawned = ["mma-fix", "kv-cache", "repo-cleanup"]
themes_parked = []
themes_completed = []
key_insight = "MMA bug likely host-side grid_dim or bar_sync scope, not fragment indexing. KV cache design can parallel with MMA fix. repo-cleanup (PTX gitignore + model dedup + examples) is independent work."

[[brainstorms]]
seq = 42
at_cycle = 167
trigger = "Brainstorm interval (4 tasks completed since last bs)"
level = "standard"
new_tasks_spawned = ["full-inference.12", "full-inference.13"]
new_themes_spawned = []
themes_parked = []
themes_completed = ["precision-fix", "model-loading", "tokenizer", "attention-scale"]
key_insight = "GPU inference now correct (gemm_f32 matches CPU f64). NaN at step 22 is next blocker — online softmax is the fix. 4 themes completed. 'Paris' criterion unreachable (model limitation), suggest user revision."

[[brainstorms]]
seq = 41
at_cycle = 163
trigger = "User demanded autonomous iteration + generation produces garbage text"
level = "deep"
new_tasks_spawned = ["full-inference.7", "full-inference.8", "full-inference.9", "full-inference.10"]
new_themes_spawned = []
themes_parked = []
themes_completed = []
key_insight = "Stop waiting for PyTorch. Self-diagnose: (1) WebFetch confirms GPT-2 predicts Paris, (2) weight transpose is #1 suspect (Conv1D [in,out] common bug), (3) per-layer diagnostics find where semantics are lost, (4) LM head position audit. Model limitation <5% — top-5 being function words is pathological."
