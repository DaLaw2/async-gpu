# Dev Loop State File
# Three-level hierarchy: Epic → Theme → Task
# ACTIVE/PENDING/PARKED items only — completed items in archive/

[meta]
created = "2026-03-11"
total_cycles = 606
completed_tasks = 724
status = "running"     # ready | running | blocked | completed

# Project North Star — the ONE thing all epics serve:
# Users write File::read → matmul → File::write in a single fn main().
# I/O walks hostcall, compute walks all-warp cooperative, both look like plain Rust.
# No GPU concepts leak. VectorWare's I/O strength + async-gpu's compute strength, unified.

# Current cycle position (RECOVER → GATE → SELECT → DISPATCH → SAVE → ROUTE)
current_step = "do.execute"
current_task_id = "coop-api.1,lib-extract.1,lib-toolchain.1"

# Sequence counters
brainstorm_seq = 113
tasks_since_brainstorm = 0   # reset after brainstorm

# ============================================================
# Epics — Tiered (T0 → T3), sorted by tier then priority
# ============================================================
# Completed epics archived: async-std, gpu-error, gpu-debug, native-warp-async,
# gpu-autonomous, gpu-perf, real-std, std-sysroot, public-api, network-io,
# formal-verification, gpu-executor, extended-std, compute-utils, compute-demo,
# gpu-channels, benchmarks, tokio-bridge, production-ready, tensor-core-gemm,
# object-detection, developer-experience, ml-framework, gpu-rag, autograd-v4,
# int8-inference, diff-physics, kernel-fusion, persistent-kernel, onnx-inference,
# kernel-fusion-v2, int4-quantization, graph-compiler, refactor, onnx-perf,
# refactor-v2, gpu-dynamic, graph-algorithms, int4-gpt2, monte-carlo, benchmark

# --- T0: Foundation (must be done first) ---

[[epics]]
id = "native-rust-dx"
title = "Native Rust DX — GPU code looks like normal Rust"
tier = 0
status = "active"
priority = "highest"
# North Star: GPU code syntax is indistinguishable from normal Rust.
#   This is the SYNTAX FOUNDATION for unified I/O + Compute — both must look native.
# Litmus Test: Show a kernel with File::read + matmul + File::write to a Rust dev —
#   they can read it without knowing it's GPU code.
success_criteria = [
    "Kernel entry uses extern \"gpu-kernel\" fn main(), no custom attribute macros needed",
    "async fn works without #[warp_cooperative] or #[warp_async] — MIR pass handles it transparently",
    "Host-side launch is one line: gpu::run(kernel_main) — no GpuRuntime/HostcallSession/KernelRegistry exposed",
    "All examples/ rewritten in native Rust style — a Rust dev can read them without GPU knowledge",
]

[[epics]]
id = "std-thread-gpu"
title = "std::thread on GPU — thread::spawn() maps to warp"
tier = 0
status = "completed"
priority = "highest"
# North Star: thread::spawn() on GPU just works — this is the I/O PILLAR of the unified kernel.
#   Sequential I/O (file read/write, println!) happens in warp-as-thread mode.
# Litmus Test: VectorWare "threads on GPU" blog example compiles and runs on async-gpu.
success_criteria = [
    "Warp 0 runs main(), other warps sleep until thread::spawn() wakes them",
    "thread::spawn() returns JoinHandle, join() blocks parent warp until child completes",
    "thread::current(), thread::sleep(), thread::yield_now() all work",
    "thread::available_parallelism() returns number of available warps",
    "Example: spawn 2+ threads doing independent work, join results, println! — identical to CPU Rust",
]

# --- T1: Core pillars ---

[[epics]]
id = "cooperative-compute"
title = "Cooperative Compute — GPU-internal all-warp parallel computation"
tier = 1
status = "completed"
priority = "high"
# North Star: This is the COMPUTE PILLAR of the unified kernel. From sequential I/O context,
#   switch to all-warp data-parallel compute, then back. I/O + Compute in one fn main().
# Litmus Test: File::read → matmul → File::write in a single kernel, no mode-switch boilerplate.
success_criteria = [
    "gpu::cooperative(closure) wakes all warps, executes closure in data-parallel mode, returns result",
    "Seamless transition: sequential code (1 warp) → cooperative (all warps) → sequential — no manual sync",
    "Existing nn ops (matmul, attention, conv2d) callable from within a kernel via cooperative",
    "Demo: read file → matmul → write result, all in one extern \"gpu-kernel\" fn main()",
]

[[epics]]
id = "structured-concurrency"
title = "Structured Concurrency — scope-based parallelism mapped to GPU hardware"
tier = 1
status = "pending"
priority = "high"
# North Star: Task scope = hardware scope. BlockScope uses shared memory, GridScope uses global.
#   Resources auto-reclaim when scope ends. Channels pick the fastest path automatically.
# Litmus Test: Users never manually choose shared vs global memory — the scope decides.
success_criteria = [
    "BlockScope and GridScope types enforce lifetime-bounded task scopes via Rust lifetimes",
    "Resources allocated within a scope auto-reclaim when scope ends (shared memory for block, global for grid)",
    "GpuChannel<T> auto-selects transport: shfl.sync (same warp), shared memory (same block), global (cross-block)",
    "Cancellation propagates down scope hierarchy — parent cancel kills all children",
    "Demo: producer-consumer pipeline using BlockScope with automatic shared memory channels",
]

[[epics]]
id = "library-api"
title = "Library API — async-gpu as a usable crate, not a demo collection"
tier = 1
status = "active"
priority = "highest"
# North Star: A user adds `async-gpu = "0.1"` to Cargo.toml and writes GPU programs
#   using a clean, documented public API. No sysroot hacks, no copy-pasting from examples.
# Litmus Test: A new user clones the repo, runs `cargo install`, and writes a working
#   GPU kernel + host program in under 30 minutes using only the docs.
success_criteria = [
    "Single `async-gpu` facade crate re-exports gpu::run/launch/custom + kernel-side runtime",
    "Public API surface is documented (rustdoc) — internal types are pub(crate)",
    "Clean separation: library crates vs demo/example vs test harness — no mixing",
    "Toolchain setup is scripted: one command gives the user a working patched toolchain",
    "Getting-started guide: from zero to running GPU kernel in documented steps",
]

[[epics]]
id = "kernel-perf"
title = "Kernel Performance — close gap to cuBLAS/cuDNN across all compute primitives"
tier = 1
status = "active"
priority = "high"
# North Star: The compute ops inside cooperative() must be FAST — this is what makes
#   async-gpu better than VectorWare. Competitive with cuBLAS/cuDNN, not just convenient.
# Litmus Test: GPT-2 inference is within 2x of a hand-optimized CUDA implementation.
success_criteria = [
    "SGEMM >= 70% of cuBLAS at 4096³",
    "Flash Attention >= 70% of cuDNN FA2 at seq=512",
    "Conv2D >= 70% of cuDNN for common shapes (ResNet, YOLO)",
    "GPT-2 end-to-end forward pass < 25ms",
]

# --- T2: Advanced ---

[[epics]]
id = "cuda-graph-scheduling"
title = "CUDA Graph Scheduling — static async pipelines compile to CUDA Graphs"
tier = 2
status = "pending"
priority = "medium"
# North Star: Users write async pipelines, the compiler automatically identifies static
#   portions and emits CUDA Graphs. No code changes needed for the speedup.
# Litmus Test: An async pipeline runs faster with zero user-facing API changes.
success_criteria = [
    "MIR pass identifies static vs dynamic branches in async state machines",
    "Static paths emit CUDA Graph nodes (kernel launch, memcpy, sync)",
    "Dynamic branches fallback to polling — no correctness regression",
    "Measurable speedup (>= 20%) on a multi-stage async pipeline vs polling-only baseline",
]

[[epics]]
id = "unified-runtime"
title = "Unified Runtime — one async fn spans CPU and GPU transparently"
tier = 2
status = "pending"
priority = "medium"
# North Star: Users write one async fn mixing I/O and compute. The runtime decides
#   what runs where. Data moves automatically. No cudaMemcpy anywhere.
# Litmus Test: A pipeline that reads a file, does matmul, writes result — user never
#   mentions "host" or "device."
success_criteria = [
    "Scheduler types (CpuScheduler, GpuScheduler, AutoScheduler) route work by affinity",
    "Data transfer (host ↔ device) is automatic — no manual cudaMemcpy",
    "AutoScheduler picks execution location based on work type and hardware capability",
    "Demo: read → compute → write pipeline with zero explicit memory management",
]

[[epics]]
id = "cross-vendor"
title = "Cross-Vendor GPU — same code runs on NVIDIA, AMD, Vulkan"
tier = 2
status = "pending"
priority = "low"
# North Star: Users write Rust GPU code once, it runs on any GPU vendor.
# Litmus Test: The same example binary runs on both NVIDIA and AMD hardware.
success_criteria = [
    "WarpPrimitive trait abstracts barrier sync, shuffle, atomics across vendors",
    "NVIDIA backend: bar.warp.sync, shfl.sync, PTX atomics",
    "AMD backend: wavefront barrier, ds_swizzle/dpp_mov, HIP atomics",
    "At least one non-trivial example runs correctly on both NVIDIA and AMD",
]

# --- T3: Exploratory ---

[[epics]]
id = "actor-model"
title = "GPU Actor Model — warp-as-actor with message passing"
tier = 3
status = "pending"
priority = "low"
# North Star: An alternative to async/await for task parallelism — each warp is an actor
#   with a mailbox, no shared mutable state, fault isolation.
# Litmus Test: A producer-consumer pipeline written with actors is shorter and clearer
#   than the equivalent async version.
success_criteria = [
    "GpuActor trait with handle_message method",
    "Mailbox in shared memory (same block) or global memory (cross block) — auto-selected",
    "Rust Send trait ensures message ownership safety at compile time",
    "Demo: multi-actor pipeline that's clearer than the async equivalent",
]

[[epics]]
id = "formal-verification"
title = "Formal Verification — MIR pass correctness for arbitrary async fn"
tier = 3
status = "pending"
priority = "low"
# North Star: Users can trust that the MIR pass won't generate wrong code for any
#   valid async fn — proven, not just tested.
# Litmus Test: A formal proof that sync insertion is sound for all reachable async states.
success_criteria = [
    "Operational semantics defined for warp-cooperative async",
    "Soundness proof: MIR pass sync insertion is sufficient for all valid async fn",
    "Completeness: async fn that would cause warp divergence rejected at compile time",
    "TLA+ model extended to cover MIR pass state transitions",
]

# --- Evergreen ---

[[epics]]
id = "codebase-health"
title = "Codebase Health — build system, CI, docs, workflow automation"
type = "evergreen"
status = "active"
priority = "low"
success_criteria = [
    "Host crates share root workspace; GPU crates use separate build due to nvptx64 target constraint",
    "Nightly toolchain version defined in one place, CI + scripts derive from it",
    "README.md accurately reflects current project state, crate map, and setup instructions",
    "Routine maintenance tasks (new crate → CI, new PTX → stub list) are automated or self-documenting",
    "Research workflow handles codebase-health tasks without full Think/Do overhead",
]

# --- Parked ---

[[epics]]
id = "classical-ml"
title = "Classical ML on GPU — decision trees, random forest, k-means"
status = "parked"
priority = "medium"
# Parked: revisit as showcase example after native-rust-dx is done
success_criteria = [
    "GPU decision tree inference: load trained tree, classify 10K+ samples in parallel",
    "GPU random forest: ensemble of 100+ trees, majority-vote, accuracy matches CPU reference",
    "GPU k-means: Lloyd's algorithm, K=10, 10K+ points, iterative convergence, speedup >= 5x over CPU",
    "Standalone example in examples/std/classical-ml/",
]

# ============================================================
# Themes (active/pending/parked only — completed → archive/)
# ============================================================
# Completed themes archived (cycle 604): thread-spawn, thread-api, perf-elem-fix
# Archived (cycle 594): onnx-fix, refactor-quality, refactor-api, onnx-cache,
# onnx-gpu-ops, refactor-v2-code, refactor-v2-perf, refactor-v2-test,
# onnx-conv-prepad, onnx-resnet-acc, onnx-mobilenet, dyn-control, graph-bfs,
# int4-integrate, mc-finance, bench-kernels, bench-e2e, perf-gemm

# --- T0: std-thread-gpu themes ---

[[themes]]
id = "std-thread-integration"
epic = "std-thread-gpu"
title = "std::thread::spawn via patched std — fix bar.sync hang"
status = "completed"
goal = "std::thread::spawn() works on GPU via patched rustc std"
success_criteria = [
    "std::thread::spawn on GPU doesn't hang (bar.sync + std init interaction fixed)",
    "Example: std::thread::spawn 2+ threads, join, println! — identical to CPU Rust",
]

# --- T0: native-rust-dx themes ---

[[themes]]
id = "native-api"
epic = "native-rust-dx"
title = "gpu::run() one-liner launcher + clean examples"
status = "completed"
goal = "Host-side launch is one line, examples look like normal Rust"
success_criteria = [
    "gpu::run(kernel_fn) wraps PTX load + launch + sync",
    "All examples/ rewritten to use gpu::run() style",
    "A Rust dev can read examples without GPU knowledge",
]

[[tasks]]
id = "std-thread-integration.1"
theme = "std-thread-integration"
title = "Investigation: debug std::thread::spawn hang — bar.sync + std init interaction"
kind = "investigation"
status = "done"
depends = []

[[tasks]]
id = "std-thread-integration.2"
theme = "std-thread-integration"
title = "Experiment: fix hang + verify std::thread::spawn example with println!"
kind = "experiment"
status = "done"
depends = ["std-thread-integration.1"]

[[tasks]]
id = "native-api.2"
theme = "native-api"
title = "Experiment: rewrite examples/ to use gpu::run() native Rust style"
kind = "experiment"
status = "done"
depends = ["native-api.1"]

[[tasks]]
id = "native-api.3"
theme = "native-api"
title = "Investigation: gpu-kernel ABI availability + migration plan"
kind = "investigation"
status = "done"
depends = []

[[tasks]]
id = "native-api.4"
theme = "native-api"
title = "Experiment: migrate all kernels from extern ptx-kernel to extern gpu-kernel"
kind = "experiment"
status = "done"
depends = ["native-api.3"]

[[tasks]]
id = "native-api.5"
theme = "native-api"
title = "Design: gpu::run_custom() builder API for multi-argument kernels"
kind = "design"
status = "done"
depends = []

[[tasks]]
id = "native-api.6"
theme = "native-api"
title = "Experiment: rewrite remaining examples with builder API + std kernel-side APIs"
kind = "experiment"
status = "done"
depends = ["native-api.5"]

# --- T1: library-api themes ---

[[themes]]
id = "lib-extract"
epic = "library-api"
title = "Extract test harness from gpu-host — pure library crate"
status = "active"
goal = "gpu-host is a pure library; test harness in separate crate"
success_criteria = [
    "main.rs + 13 tests_*.rs + bench_harness.rs moved to crates/test/gpu-test-harness/",
    "gpu-host has no [[bin]] target, default features = []",
]

[[themes]]
id = "lib-facade"
epic = "library-api"
title = "async-gpu facade crate with curated re-exports"
status = "pending"
goal = "Single async-gpu crate is the user's entry point"
success_criteria = [
    "crates/async-gpu/ re-exports gpu::run/launch/custom + error types",
    "At least one example depends on async-gpu instead of gpu-host",
]

[[themes]]
id = "lib-cleanup"
epic = "library-api"
title = "Clean public API surface — hide internals, remove dead code"
status = "pending"
goal = "cargo doc produces useful API docs"
success_criteria = [
    "Internal modules pub(crate), protocol constants in submodules",
    "cargo doc --no-deps output is clean and useful",
]

[[themes]]
id = "lib-toolchain"
epic = "library-api"
title = "One-command toolchain setup"
status = "pending"
goal = "scripts/setup.sh gives working environment"
success_criteria = [
    "setup.sh with quick/full/check modes",
    "Smoke test compiles and runs a trivial kernel",
]

[[themes]]
id = "lib-docs"
epic = "library-api"
title = "Getting-started guide + rustdoc"
status = "pending"
goal = "New user writes working GPU program in 30 min"
success_criteria = [
    "Getting-started guide in docs/ or README",
    "rustdoc with doc examples for all public types",
]

[[tasks]]
id = "lib-extract.1"
theme = "lib-extract"
title = "Investigation: inventory test harness deps on gpu-host internals"
kind = "investigation"
status = "done"
depends = []

[[tasks]]
id = "lib-extract.2"
theme = "lib-extract"
title = "Experiment: create gpu-test-harness crate, move main.rs + tests_*.rs"
kind = "experiment"
status = "done"
depends = ["lib-extract.1"]

[[tasks]]
id = "lib-extract.3"
theme = "lib-extract"
title = "Experiment: remove [[bin]] from gpu-host, change default features to []"
kind = "experiment"
status = "done"
depends = ["lib-extract.2"]

[[tasks]]
id = "lib-extract.4"
theme = "lib-extract"
title = "Experiment: verify test harness still passes from new location"
kind = "experiment"
status = "done"
depends = ["lib-extract.3"]

[[tasks]]
id = "lib-facade.1"
theme = "lib-facade"
title = "Investigation: design async-gpu facade structure and re-export surface"
kind = "investigation"
status = "done"
depends = ["lib-extract.1"]

[[tasks]]
id = "lib-facade.2"
theme = "lib-facade"
title = "Experiment: create crates/async-gpu/ with curated re-exports"
kind = "experiment"
status = "done"
depends = ["lib-facade.1"]

[[tasks]]
id = "lib-toolchain.1"
theme = "lib-toolchain"
title = "Investigation: minimum user setup requirements"
kind = "investigation"
status = "done"
depends = []

[[tasks]]
id = "lib-toolchain.2"
theme = "lib-toolchain"
title = "Experiment: create scripts/setup.sh with quick/full/check modes"
kind = "experiment"
status = "parked"
depends = ["lib-toolchain.1"]

[[tasks]]
id = "kernel-entry-dx"
epic = "native-rust-dx"
title = "Zero-boilerplate kernel entry — implicit hostcall injection"
status = "active"
goal = "extern 'gpu-kernel' fn kernel_main() with NO parameters, NO manual init"
success_criteria = [
    "Kernel entry fn kernel_main() — no buf/result parameters",
    "stdio_init + gpu_libc_io_init happen automatically (compiler or runtime injection)",
    "gpu_main_poll wrapper is implicit — user writes sequential code directly in fn body",
    "Existing kernels in gpu-kernel-std work with the new entry style",
]

[[tasks]]
id = "kernel-entry.1"
theme = "kernel-entry-dx"
title = "Investigation: how VectorWare injects hostcall buf — compiler transform vs runtime linker"
kind = "investigation"
status = "done"
depends = []

[[tasks]]
id = "kernel-entry.2"
theme = "kernel-entry-dx"
title = "Experiment: implement implicit hostcall injection for zero-param kernel entry"
kind = "experiment"
status = "done"
depends = ["kernel-entry.1"]

[[tasks]]
id = "kernel-entry.3"
theme = "kernel-entry-dx"
title = "Experiment: rewrite gpu-kernel-std kernels to use zero-param entry"
kind = "experiment"
status = "pending"
depends = ["kernel-entry.2"]

[[tasks]]
id = "lib-cleanup.1"
theme = "lib-cleanup"
title = "Investigation: audit all files/functions — are they in the right crate?"
kind = "investigation"
status = "done"
depends = ["lib-extract.2", "lib-facade.2"]

[[tasks]]
id = "lib-cleanup.2"
theme = "lib-cleanup"
title = "Experiment: move model/yolo/tokenizer demo code out of gpu-host"
kind = "experiment"
status = "blocked"
depends = ["lib-extract.2"]
blocked_reason = "50+ call sites across test harness + examples. Need gpu-models crate — larger refactor."

[[tasks]]
id = "lib-cleanup.3"
theme = "lib-cleanup"
title = "Experiment: migrate warp.rs off warp-macro DSL, then remove warp-macro crate"
kind = "experiment"
status = "done"
depends = []

[[tasks]]
id = "lib-cleanup.4"
theme = "lib-cleanup"
title = "Experiment: merge gpu-kernel + gpu-kernel-std into single kernel crate"
kind = "experiment"
status = "pending"
depends = ["lib-extract.2"]

# --- T1: cooperative-compute themes ---

[[themes]]
id = "coop-api"
epic = "cooperative-compute"
title = "Cooperative API ergonomics — safe data passing + return values"
status = "active"
goal = "cooperative_map() with data passing, no global atomics"
success_criteria = [
    "cooperative_map(in_ptr, out_ptr, n, closure) passes data without global atomics",
    "Seamless sequential → cooperative → sequential with zero ceremony",
]

[[themes]]
id = "coop-compute"
epic = "cooperative-compute"
title = "Kernel-side compute library — matmul callable from cooperative()"
status = "active"
goal = "Matmul and basic ops callable from within a kernel"
success_criteria = [
    "Naive kernel-side matmul (triple loop, row-partitioned across warps)",
    "cooperative_map-based matmul callable from sequential kernel context",
]

[[themes]]
id = "coop-demo"
epic = "cooperative-compute"
title = "Unified I/O+Compute demo — File::read → matmul → File::write"
status = "active"
goal = "End-to-end demo proving the vision in one kernel"
success_criteria = [
    "Read two matrix files, cooperative matmul, write result — single kernel",
    "Host-side verification matches CPU reference",
]

[[tasks]]
id = "coop-api.1"
theme = "coop-api"
title = "Investigation: cooperative() data passing without global atomics"
kind = "investigation"
status = "done"
depends = []

[[tasks]]
id = "coop-api.2"
theme = "coop-api"
title = "Experiment: implement cooperative_map + cooperative_reduce"
kind = "experiment"
status = "done"
depends = ["coop-api.1"]

[[tasks]]
id = "coop-api.3"
theme = "coop-api"
title = "Experiment: rewrite unified_io_compute with cooperative_map"
kind = "experiment"
status = "done"
depends = ["coop-api.2"]

[[tasks]]
id = "coop-compute.1"
theme = "coop-compute"
title = "Investigation: kernel-side matmul architecture design"
kind = "investigation"
status = "done"
depends = ["coop-api.1"]

[[tasks]]
id = "coop-compute.2"
theme = "coop-compute"
title = "Experiment: naive kernel-side matmul MVP via cooperative_map"
kind = "experiment"
status = "done"
depends = ["coop-compute.1", "coop-api.2"]

[[tasks]]
id = "coop-compute.3"
theme = "coop-compute"
title = "Experiment: tiled kernel-side matmul for performance"
kind = "experiment"
status = "pending"
depends = ["coop-compute.2"]

[[tasks]]
id = "coop-demo.1"
theme = "coop-demo"
title = "Experiment: litmus test — File::read → matmul → File::write in one kernel"
kind = "experiment"
status = "done"
depends = ["coop-compute.2", "coop-api.3"]

[[tasks]]
id = "coop-demo.2"
theme = "coop-demo"
title = "Experiment: multi-stage pipeline demo (read → matmul → softmax → write)"
kind = "experiment"
status = "pending"
depends = ["coop-demo.1"]

# --- kernel-perf themes ---

[[themes]]
id = "perf-attention"
epic = "kernel-perf"
title = "FlashAttention-2 — MMA tiled GEMM + online softmax"
status = "active"
goal = "Attention throughput >= 50% of cuDNN FA2"
success_criteria = [
    "Q tile in shared memory for cooperative tiled GEMM",
    "Score GEMM (Q·K^T) via MMA or tiled register-blocked matmul",
    "P·V accumulation via tiled GEMM (not scalar loop)",
    "Warp-level parallel softmax reduction",
    "seq=512, 12 heads: throughput >= 1200 GFLOPS",
]

[[themes]]
id = "perf-conv"
epic = "kernel-perf"
title = "High-performance Conv2D — Winograd + direct conv"
status = "active"
goal = "Conv2D >= 60% of cuDNN for common shapes"
success_criteria = [
    "Winograd F(4×4, 3×3) for 3×3 convolutions (2.25x FLOPs reduction)",
    "Direct conv kernel for non-3×3 sizes (shared memory tiling)",
    "im2col completely eliminated from forward path",
    "GPU-side batch processing (no PCIe round-trips)",
    "ResNet layer2 (64→128, 32×32, k=3): >= 1200 GFLOPS",
]

[[themes]]
id = "perf-layernorm"
epic = "kernel-perf"
title = "High-performance LayerNorm — single-pass + vectorized"
status = "active"
goal = "LayerNorm >= 180 GB/s (60% of peak bandwidth)"
success_criteria = [
    "Single-pass Welford algorithm (mean + variance in one read)",
    "256 threads per block with shared memory two-level reduction",
    "float4 coalesced loads (128-bit transactions)",
    "Fused LayerNorm + residual add variant",
    "128×768 matrix: >= 180 GB/s",
]

[[themes]]
id = "perf-elementwise"
epic = "kernel-perf"
title = "Vectorized elementwise ops — float4 + multi-element per thread"
status = "pending"
goal = "elementwise >= 240 GB/s, GELU >= 200 GB/s (80%+ of peak)"
success_criteria = [
    "float4 vectorized loads/stores for all elementwise ops",
    "4-8 elements per thread for latency hiding",
    "GELU fast approximation: x * sigmoid(1.702x)",
    "elementwise_add >= 240 GB/s, gelu >= 200 GB/s",
]

[[themes]]
id = "perf-e2e"
epic = "kernel-perf"
title = "End-to-end validation — GPT-2 < 35ms, YOLO < 80ms"
status = "pending"
goal = "Verify all kernel improvements compound to model-level speedup"
success_criteria = [
    "GPT-2 forward pass (seq=128) < 35ms (from 221ms)",
    "YOLOv8-nano inference < 100ms (from 374ms)",
    "No accuracy regression on any model",
]

[[themes]]
id = "perf-fusion"
epic = "kernel-perf"
title = "Op Fusion — fuse LayerNorm+residual, reduce kernel launches"
status = "active"
goal = "Reduce GPT-2 per-block overhead by fusing small ops"
success_criteria = [
    "Fused LayerNorm + residual add kernel (1 kernel instead of 2)",
    "GPT-2 per-block time reduced by >= 0.3ms from fusion",
]

[[themes]]
id = "perf-gemm-v4"
epic = "kernel-perf"
title = "SGEMM V4 — float4 vectorized loads + cp.async pipeline"
status = "active"
goal = "SGEMM >= 70% cuBLAS (~1960 GFLOPS at 4096³)"
success_criteria = [
    "ld.global.v4.f32 for A and B tile loading",
    "cp.async.ca.shared.global 3-stage pipeline",
    "4096³ >= 1960 GFLOPS (70% of cuBLAS 2800)",
]

[[themes]]
id = "perf-attn-v3"
epic = "kernel-perf"
title = "Attention V3 — warp-cooperative tiled GEMM for Q·K^T and P·V"
status = "active"
goal = "Flash Attention >= 70% of cuDNN FA2"
success_criteria = [
    "Score GEMM via cooperative thread-tile (not scalar dot product)",
    "P·V via cooperative thread-tile accumulation",
    "seq=512, 12 heads: >= 70% of cuDNN FA2 throughput",
]

[[themes]]
id = "perf-conv-wino"
epic = "kernel-perf"
title = "Conv2D Winograd — F(4×4,3×3) for large spatial convolutions"
status = "pending"
goal = "Conv2D 3×3 >= 70% cuDNN for 56×56+ spatial sizes"
success_criteria = [
    "Winograd F(4×4,3×3) transform + batched GEMM + inverse transform",
    "64→64 @ 56×56: >= 70% of cuDNN 1439 GFLOPS",
    "No accuracy regression vs im2col path",
]

[[themes]]
id = "perf-int8-tiled"
epic = "kernel-perf"
title = "INT8 tiled dp4a — shared memory tiled INT8 GEMM for competitive inference"
status = "parked"
goal = "INT8 GEMM via tiled dp4a >= 2x f32 FMA throughput"
# From deferred int8-inference epic. Naive dp4a is 7-10x slower than tiled f32.
# Tiled dp4a with shared memory should unlock 2-4x speedup over f32.
success_criteria = [
    "Tiled dp4a kernel with shared memory staging (same tiling strategy as SGEMM V4)",
    "INT8 GEMM throughput >= 2x f32 FMA at 4096x4096",
    "GPT-2 full INT8 inference produces coherent text (top-5 match f32 for >= 2/3 prompts)",
]

[[themes]]
id = "perf-gemm-fusion"
epic = "kernel-perf"
title = "Fused GEMM+activation in nn API — fused_prepadded_b path"
status = "parked"
goal = "Linear layer dispatches fused GEMM+bias+activation, measurable GPT-2 speedup"
# From deferred kernel-fusion + kernel-fusion-v2 epics. fused kernels exist but
# pre-padded B path is faster than unfused; needs fused_prepadded_b to combine both.
success_criteria = [
    "fused_prepadded_b kernel: pre-padded weight matrix + GEMM + bias + activation in one launch",
    "nn::Linear detects subsequent activation and dispatches fused path automatically",
    "GPT-2 per-token latency reduced >= 10% from GEMM fusion (vs pre-padded unfused baseline)",
]

# ============================================================
# Tasks (active/pending only — done → archive/)
# ============================================================
# Done tasks archived (cycle 604): thread-spawn.1, .2, .3, native-api.1,
# perf-attn-v3.1, .2, perf-conv.1, perf-fusion.1, perf-elem-fix.1

# Orphaned parked tasks removed (cycle 604): warp-batch-io.2-4, cml-tree.1-3, cml-kmeans.1
# (parent epics archived — brainstorm will recreate if epic reactivates)

# --- kernel-perf: attention ---

[[tasks]]
id = "perf-attn-v3.3"
theme = "perf-attn-v3"
title = "Experiment: cooperative P·V accumulation + online softmax"
kind = "experiment"
status = "pending"
depends = ["perf-attn-v3.2"]

[[tasks]]
id = "perf-attn-v3.4"
theme = "perf-attn-v3"
title = "Experiment: benchmark, iterate until ≥70% cuDNN, integrate"
kind = "experiment"
status = "pending"
depends = ["perf-attn-v3.3"]

[[tasks]]
id = "perf-attention.3"
theme = "perf-attention"
title = "Experiment: tiled P·V accumulation + cp.async KV prefetch + warp softmax"
kind = "experiment"
status = "pending"
depends = ["perf-attention.2"]

[[tasks]]
id = "perf-attention.4"
theme = "perf-attention"
title = "Experiment: benchmark + integrate into nn::ops::multi_head_flash_attention"
kind = "experiment"
status = "pending"
depends = ["perf-attention.3"]

# --- kernel-perf: conv ---

[[tasks]]
id = "perf-conv.2"
theme = "perf-conv"
title = "Experiment: implement Winograd 3×3 conv kernel with shared memory transforms"
kind = "experiment"
status = "pending"
depends = ["perf-conv.1", "perf-gemm.3"]

[[tasks]]
id = "perf-conv.3"
theme = "perf-conv"
title = "Experiment: direct conv kernel for 1×1, 5×5, 7×7 with register tiling"
kind = "experiment"
status = "pending"
depends = ["perf-gemm.3"]

[[tasks]]
id = "perf-conv.4"
theme = "perf-conv"
title = "Experiment: GPU-side batched conv (eliminate D2H/H2D round-trips)"
kind = "experiment"
status = "pending"
depends = ["perf-conv.2"]

[[tasks]]
id = "perf-conv.5"
theme = "perf-conv"
title = "Experiment: benchmark all conv shapes, integrate into nn::ops::conv2d"
kind = "experiment"
status = "pending"
depends = ["perf-conv.2", "perf-conv.3", "perf-conv.4"]

# --- kernel-perf: layernorm ---

[[tasks]]
id = "perf-layernorm.2"
theme = "perf-layernorm"
title = "Experiment: float4 vectorized loads + fused residual-add variant"
kind = "experiment"
status = "pending"
depends = ["perf-layernorm.1"]

[[tasks]]
id = "perf-layernorm.3"
theme = "perf-layernorm"
title = "Experiment: benchmark + integrate into nn::ops::layer_norm"
kind = "experiment"
status = "pending"
depends = ["perf-layernorm.2"]

# --- kernel-perf: elementwise ---

[[tasks]]
id = "perf-elementwise.2"
theme = "perf-elementwise"
title = "Experiment: benchmark all elementwise ops, integrate into nn::ops"
kind = "experiment"
status = "pending"
depends = ["perf-elementwise.1"]

# --- kernel-perf: fusion ---

[[tasks]]
id = "perf-fusion.2"
theme = "perf-fusion"
title = "Experiment: integrate fused LN+residual into GPT-2, benchmark"
kind = "experiment"
status = "pending"
depends = ["perf-fusion.1"]

# --- kernel-perf: GEMM V4 ---

[[tasks]]
id = "perf-gemm-v4.2"
theme = "perf-gemm-v4"
title = "Experiment: cp.async 3-stage pipeline (SM80+ async copy)"
kind = "experiment"
status = "pending"
depends = ["perf-gemm-v4.1"]

[[tasks]]
id = "perf-gemm-v4.3"
theme = "perf-gemm-v4"
title = "Experiment: benchmark V4, integrate, verify ≥70% cuBLAS"
kind = "experiment"
status = "pending"
depends = ["perf-gemm-v4.2"]

# --- kernel-perf: conv winograd ---

[[tasks]]
id = "perf-conv-wino.1"
theme = "perf-conv-wino"
title = "Experiment: implement Winograd input/filter/output transforms"
kind = "experiment"
status = "pending"
depends = ["perf-conv.1"]

[[tasks]]
id = "perf-conv-wino.2"
theme = "perf-conv-wino"
title = "Experiment: full Winograd conv2d pipeline + correctness test"
kind = "experiment"
status = "pending"
depends = ["perf-conv-wino.1"]

[[tasks]]
id = "perf-conv-wino.3"
theme = "perf-conv-wino"
title = "Experiment: benchmark all shapes, iterate until ≥70% cuDNN"
kind = "experiment"
status = "pending"
depends = ["perf-conv-wino.2"]

# --- kernel-perf: end-to-end ---

[[tasks]]
id = "perf-e2e.1"
theme = "perf-e2e"
title = "Experiment: GPT-2 end-to-end benchmark with all optimized kernels"
kind = "experiment"
status = "pending"
depends = ["perf-gemm.5", "perf-attention.4", "perf-layernorm.3", "perf-elementwise.2"]

[[tasks]]
id = "perf-e2e.2"
theme = "perf-e2e"
title = "Experiment: YOLOv8-nano + ResNet-18 benchmark with optimized conv + elementwise"
kind = "experiment"
status = "pending"
depends = ["perf-conv.5", "perf-elementwise.2"]

# ============================================================
# Brainstorms (last 2 — older → archive/)
# ============================================================

# Brainstorm 109 archived (cycle 535, onnx-inference — epic now completed/archived)

[[brainstorms]]
seq = 110
at_cycle = 560
trigger = "User requested: what epics to create, which to reopen, strategic direction"
level = "deep"
new_tasks_spawned = ["cml-tree.1", "cml-tree.2", "cml-tree.3", "cml-kmeans.1", "dyn-control.1", "dyn-control.2", "dyn-control.3", "graph-bfs.1", "graph-bfs.2", "graph-bfs.3", "int4-integrate.1", "mc-finance.1", "mc-finance.2", "mc-finance.3"]
new_themes_spawned = ["cml-tree", "cml-kmeans", "dyn-control", "graph-bfs", "int4-integrate", "mc-finance"]
new_epics_spawned = ["classical-ml", "gpu-dynamic", "graph-algorithms", "int4-gpt2", "monte-carlo"]
themes_parked = []
themes_completed = []
key_insight = "NN axis saturated. Breadth wins where async/await adds unique value. classical-ml + gpu-dynamic + graph-algorithms are top 3."

# Brainstorm 110 archived (cycle 560)

[[brainstorms]]
seq = 111
at_cycle = 605
trigger = "Proactive (tasks>=15), std-thread-gpu epic completed, native-rust-dx verification failed 2/4"
level = "standard"
new_tasks_spawned = ["native-api.3", "native-api.4", "native-api.5", "native-api.6"]
new_themes_spawned = []
new_epics_spawned = []
themes_parked = []
themes_completed = ["std-thread-integration"]
key_insight = "std-thread-gpu cleared T0. native-rust-dx needs gpu-kernel ABI migration + builder API for remaining examples. User chose to keep criteria, not weaken them."

[[brainstorms]]
seq = 112
at_cycle = 607
trigger = "T0 cleared, T1 activation — cooperative-compute epic needs themes/tasks"
level = "standard"
new_tasks_spawned = ["coop-api.1", "coop-api.2", "coop-api.3", "coop-compute.1", "coop-compute.2", "coop-compute.3", "coop-demo.1", "coop-demo.2"]
new_themes_spawned = ["coop-api", "coop-compute", "coop-demo"]
new_epics_spawned = []
themes_parked = []
themes_completed = ["native-api", "std-thread-integration"]
key_insight = "cooperative() prototype works but has 3 gaps: API ergonomics (global atomics), kernel-side compute (no callable matmul), full demo. Critical path: coop-api.1 → .2 → coop-compute.2 → coop-demo.1."

[[brainstorms]]
seq = 113
at_cycle = 607
trigger = "User feedback: we only write demos, not a usable library. library-api epic created."
level = "standard"
new_tasks_spawned = ["lib-extract.1", "lib-extract.2", "lib-extract.3", "lib-extract.4", "lib-facade.1", "lib-facade.2", "lib-toolchain.1", "lib-toolchain.2"]
new_themes_spawned = ["lib-extract", "lib-facade", "lib-cleanup", "lib-toolchain", "lib-docs"]
new_epics_spawned = ["library-api"]
themes_parked = []
themes_completed = []
key_insight = "79% of gpu-host/src (23,735 lines) is test harness inside the library crate. Extract to gpu-test-harness, create async-gpu facade, clean pub API, single setup script."
