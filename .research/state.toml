# Autonomous Research Loop State File
# Three-level hierarchy: Epic → Theme → Task
# Epics are user-defined high-level goals; Themes are research directions; Tasks are actionable items within a theme.

[meta]
created = "2026-03-11"
total_cycles = 275     # Monotonic counter, incremented after each completed phase
completed_tasks = 275
status = "running"     # ready | running | blocked | completed

# Current cycle position within Think/Do/Check/Adapt
current_mode = "do"
current_step = "do.save"
current_task_id = ""
last_summary = "async-yield.3 DONE: data pipeline demo (read→compute→write) with real async hostcall Futures + #[warp_cooperative]. PTX has 7x bar.warp.sync + 1x shfl.sync. All 3 async-yield theme criteria met. All 4 async-std epic criteria met."

# Sequence counters for unique file naming across iterations
brainstorm_seq = 75
review_seq = 5

# Brainstorm trigger control (event-driven, no fixed interval)

# ============================================================
# Epics (active only — completed epics archived in archive/)
# ============================================================

[[epics]]
id = "async-std"
title = "Async Std on GPU — real async/await with patched rustc + patched std"
status = "active"
priority = "high"
success_criteria = [
    "Async hostcall: gpu_hostcall_request returns impl Future — warp yields to scheduler during I/O wait instead of spin-blocking",
    "PAL async bridge: patched std I/O (write/read/open/close) calls async hostcall under #[warp_cooperative] context",
    "Practical demo: #[warp_cooperative] async fn runs data pipeline (read file → compute → write file) with I/O yielding",
    "Codebase reorganized into categorized subdirectories (core/kernel/macro/test)",
]

[[epics]]
id = "gpu-error"
title = "GPU-side error handling — Result propagation from GPU to host"
status = "completed"
priority = "medium"
success_criteria = [
    "GPU kernel can use `?` operator and return Result<T, E> to host",
    "Errors propagate across async boundaries (hostcall errors → Result, not panic)",
    "Host receives structured error info (error type + message), not just trap/exit code",
    "std::io and std::fs errors on GPU produce proper std::io::Error, not panic",
]
# ALL 4 CRITERIA MET — closed by user 2026-03-14

[[epics]]
id = "gpu-debug"
title = "GPU Debugging & Observability — structured tracing, assertions, flight recorder"
status = "completed"
priority = "high"
success_criteria = [
    "gpu_trace!() macro emits structured events with thread/block/warp ID + timestamp to host",
    "gpu_assert!() sends diagnostic message + thread coords to host before trap",
    "Post-mortem flight recorder dumps last N trace events when kernel crashes",
    "Conditional compilation: tracing compiles out for zero overhead in release",
]

[[epics]]
id = "native-warp-async"
title = "Native Warp-Cooperative Async/Await — compiler-level SIMT async"
status = "completed"
priority = "high"
success_criteria = [
    "Standard impl Future polled warp-cooperatively (lane 0 polls, shfl broadcast)",
    "#[warp_async] accepts async fn syntax with .await and ? operator",
    "#[warp_cooperative] async fn compiles via rustc MIR pass and runs on GPU",
]
# ALL 3 CRITERIA MET — closed by user 2026-03-14

[[epics]]
id = "gpu-autonomous"
title = "GPU as Autonomous Compute Environment v3 — native warp-cooperative async/await"
status = "completed"
priority = "high"
success_criteria = [
    "Multi-command kernel: processes 3+ command types (COMPUTE, PRINT, EXIT) from mapped memory command buffer in a single launch, TDR-safe",
    "Cross-launch pipeline: two kernels share device buffer (A writes → B reads, zero host copy), with minimal Pipeline API",
    "Data-dependent iteration: kernel autonomously loops (read → process → check convergence → iterate or finalize), pattern not yet demonstrated",
    "Hostcall session persistence: HostcallSession keeps listener + fd table + buffer alive across multiple kernel launches, packet pool reinit between launches",
    "Standard impl Future can be polled warp-cooperatively (lane 0 polls, broadcast result via shfl.sync)",
    "#[warp_async] accepts async fn syntax with .await and ? operator",
    "#[warp_cooperative] async fn compiles via rustc MIR pass and runs correctly on GPU",
]
# ALL 7 CRITERIA MET — closed by user 2026-03-14

[[epics]]
id = "gpu-perf"
title = "GPU Inference Performance Optimization"
status = "completed"
priority = "high"
success_criteria = [
    "KV cache eliminates redundant recomputation — only new token passes through model per step",
    "PARKED: Per-token generation latency < 20ms (down from ~143ms)",
    "PARKED: BF16/TF32 MMA inference matches f32 FMA output (top-5 agreement for 3+ prompts)",
    "PARKED: Mixed precision pipeline: GEMM via Tensor Core, everything else f32",
]
# Criterion 1 DONE. Criteria 2-4 PARKED (out of scope). Closed by user 2026-03-14.

[[epics]]
id = "real-std"
title = "Real std on GPU — replace macros with actual Rust std"
status = "completed"
priority = "high"
success_criteria = [
    "GPU kernel can `use std::io::Write` and call `println!()` without custom macros",
    "Vec, String, Box work on GPU via #[global_allocator] backed by hostcall-aware allocator",
    "std::fs::File::create() and write_all() work on GPU via hostcall",
    "std::io::stdin().read_line() works on GPU via hostcall",
    "PAL/libc facade routes std sys calls to hostcall framework",
]
# ALL 5 CRITERIA MET — closed by user 2026-03-14

[[epics]]
id = "codebase-health"
title = "Codebase Health — build system, CI, docs, workflow automation"
type = "evergreen"  # Ongoing quality epic — work on when no other tasks are ready
status = "active"
priority = "low"
success_criteria = [
    "Host crates share root workspace; GPU crates use separate build due to nvptx64 target constraint",
    "Nightly toolchain version defined in one place, CI + scripts derive from it",
    "README.md accurately reflects current project state, crate map, and setup instructions",
    "Routine maintenance tasks (new crate → CI, new PTX → stub list) are automated or self-documenting",
    "Research workflow handles codebase-health tasks (lint fix, doc, CI update) without full Think/Do/Check overhead",
]

[[epics]]
id = "public-api"
title = "Public API — Host SDK + Reusable GPU Kernel Library"
type = "evergreen"  # Ongoing quality epic — work on when no other tasks are ready
status = "active"
priority = "low"
success_criteria = [
    "gpu-host is a library crate with documented public API (GpuDevice, KernelBuilder, HostcallRuntime)",
    "A standalone example program uses gpu-host as a dependency to run a GPU kernel",
    "Build system automated — single cargo command compiles host + kernel",
    "At least 3 working examples (hello-gpu, async-io, compute) with documentation",
]

# ============================================================
# Themes (active only — completed/parked archived in archive/)
# ============================================================

[[themes]]
id = "async-yield"
epic = "async-std"
title = "Async yield for hostcall — I/O yields warp instead of spin-blocking"
status = "completed"
# ALL 3 CRITERIA MET — completed 2026-03-15
goal = "gpu_hostcall_request returns Future; warp yields during I/O wait so other warps can run compute on the SM"
success_criteria = [
    "gpu_hostcall_request_async() returns impl Future that checks CONTROL_READY once per poll",
    "PAL layer calls async hostcall under #[warp_cooperative] context",
    "Demo: data pipeline (read→compute→write) with I/O yielding, verified on GPU",
]

[[themes]]
id = "codebase-restructure"
epic = "async-std"
title = "Reorganize codebase into categorized subdirectories"
status = "completed"
goal = "Crates organized into core/kernel/macro/test, test-warp-crate moved to examples/"
success_criteria = [
    "Crates organized into crates/core/, crates/kernel/, crates/macro/, crates/test/",
    "test-warp-crate moved to examples/warp-cooperative/",
    "CI and scripts updated to reflect new structure",
]
# ALL 3 CRITERIA MET — completed 2026-03-15

# ============================================================
# Tasks (pending/active only — done tasks archived in archive/)
# ============================================================

[[tasks]]
id = "async-yield.1"
theme = "async-yield"
title = "Investigation: design async hostcall mechanism — how does a sync PAL fn yield?"
kind = "investigation"
status = "done"
depends = []
questions = [
    "How can a sync fn (std::io::Write::write) yield to warp scheduler? It returns io::Result, not Poll.",
    "Can the MIR pass transform sync I/O calls into .await points automatically?",
    "Alternative: can gpu_hostcall_request itself yield via inline PTX (nanosleep/bar.warp.sync) between spin iterations?",
    "What does 'yield to other warps' mean on GPU — is nanosleep sufficient or do we need actual deschedule?",
    "How does CUDA cooperative groups / yield() work at PTX level?",
]

[[tasks]]
id = "async-yield.2"
theme = "async-yield"
title = "Experiment: prototype async hostcall Future in gpu-runtime"
kind = "experiment"
status = "done"
depends = ["async-yield.1"]

[[tasks]]
id = "async-yield.3"
theme = "async-yield"
title = "Experiment: data pipeline demo — read→compute→write with async I/O"
kind = "experiment"
status = "done"
depends = ["async-yield.2"]


# ============================================================
# Brainstorms (last 3 only — older brainstorms archived)
# ============================================================

[[brainstorms]]
seq = 75
at_cycle = 274
trigger = "toolchain-auto theme completed — assess remaining work"
level = "quick"
new_tasks_spawned = []
new_themes_spawned = []
themes_parked = []
themes_completed = ["toolchain-auto"]
key_insight = "async-std epic 4/6 criteria met (async hostcall, toolchain build, PTX postprocess, codebase reorg). Remaining: criterion 2 (PAL bridge) is satisfied by the async hostcall Futures themselves — they ARE the bridge. Criterion 3 (demo) blocked on user building patched toolchain. Setting awaiting_user."

[[brainstorms]]
seq = 74
at_cycle = 271
trigger = "user direction pivot — async yield not warp batching"
level = "quick"
new_tasks_spawned = ["async-yield.1", "async-yield.2", "async-yield.3"]
new_themes_spawned = ["async-yield"]
themes_parked = []
themes_completed = ["codebase-restructure"]
key_insight = "Warp-batching println (Approach C) is a debug optimization, not real use case. Real value: I/O yields warp to scheduler so other warps run compute. Need async hostcall Future. Core challenge: sync PAL fn returning io::Result can't return Poll — need MIR-level or runtime-level yield mechanism."

[[brainstorms]]
seq = 73
at_cycle = 270
trigger = "async-std epic kickoff — no themes or tasks"
level = "standard"
new_tasks_spawned = ["warp-io.1", "warp-io.2", "toolchain-auto.1", "toolchain-auto.2", "codebase-restructure.1", "codebase-restructure.2"]
new_themes_spawned = ["warp-io", "toolchain-auto", "codebase-restructure"]
themes_parked = []
themes_completed = []
key_insight = "Spin-wait in gpu_hostcall_request is the core blocker. Three approaches analyzed: (A) async PAL — too invasive, (B) yield from spin loop — doesn't help with sync API, (C) activemask-based warp batching — most practical. Lane 0 does hostcall, others wait via bar.warp.sync. Same pattern as warp_print! but automatic."

[[brainstorms]]
seq = 72
at_cycle = 270
trigger = "warp-verify theme completed — epic assessment"
level = "quick"
new_tasks_spawned = []
new_themes_spawned = []
themes_parked = []
themes_completed = ["warp-verify"]
key_insight = "All high-priority epics fully satisfied: native-warp-async (3/3), gpu-autonomous (7/7), gpu-perf (1/1 active + 3 parked), real-std (5/5). 4 epics ready for user close. Only evergreen epics (codebase-health, public-api) remain. Research loop at natural stopping point."

[[brainstorms]]
seq = 71
at_cycle = 268
trigger = "rustc-build theme completed — no active themes"
level = "quick"
new_tasks_spawned = ["warp-verify.1", "warp-verify.2"]
new_themes_spawned = ["warp-verify"]
themes_parked = []
themes_completed = ["rustc-build"]
key_insight = "Patched compiler verified: activemask + bar.warp.sync in PTX. Trivial async fn has no dispatch switch so no shfl.sync. Need multi-await test to trigger shfl.sync broadcast. Then GPU execution test."

[[brainstorms]]
seq = 70
at_cycle = 266
trigger = "rustc-impl theme completed + gen-rustc-patches.sh done — no active themes"
level = "quick"
new_tasks_spawned = ["rustc-build.1", "rustc-build.2"]
new_themes_spawned = ["rustc-build"]
themes_parked = []
themes_completed = ["rustc-impl", "data-iter"]
key_insight = "MIR pass code done, patch scripts done. Next step: actually build the patched compiler and test it. Clone fresh rustc, apply patches, x.py build compiler. If it compiles, test with a simple warp_cooperative async fn."
