[package]
name = "gpu-test-macro"
version = "0.1.0"
edition = "2021"
description = "Proc macro for #[gpu_test] — compiles test function to GPU kernel runner"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full", "extra-traits"] }
quote = "1"
proc-macro2 = "1"
