[workspace]

[package]
name = "async-io-host"
version = "0.1.0"
edition = "2021"
description = "Async I/O — GPU file operations and data pipelines"

[dependencies]
gpu-host = { path = "../../../../crates/core/gpu-host", default-features = false }
