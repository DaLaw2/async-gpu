[workspace]

[package]
name = "async-io-host"
version = "0.1.0"
edition = "2021"
description = "Async I/O — multi-step file I/O from GPU using gpu-host SDK"

[dependencies]
cudarc = { version = "0.12", features = ["cuda-12060"] }
gpu-host = { path = "../../../../crates/core/gpu-host", default-features = false }
