[workspace]

[package]
name = "hello-gpu-host"
version = "0.1.0"
edition = "2021"
description = "Hello GPU — native Rust API for GPU compute, I/O, and threading"

[dependencies]
async-gpu = { path = "../../../../crates/async-gpu" }
