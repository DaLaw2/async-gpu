[workspace]

[package]
name = "tcp-echo-host"
version = "0.1.0"
edition = "2021"
description = "TCP Echo -- GPU-initiated TCP networking via gpu-host SDK"

[dependencies]
cudarc = { version = "0.12", features = ["cuda-12060"] }
gpu-host = { path = "../../../../crates/core/gpu-host", default-features = false }
