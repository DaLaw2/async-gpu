[workspace]

[package]
name = "warp-cooperative"
version = "0.1.0"
edition = "2021"
description = "Demo: cooperative compute on GPU — all warps work in parallel"

[dependencies]
async-gpu = { path = "../../../crates/async-gpu" }
