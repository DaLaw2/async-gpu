[workspace]

[package]
name = "auto-tuning"
version = "0.1.0"
edition = "2021"
description = "AutoTuner — warmup-based block size search for optimal GPU kernel performance"

[dependencies]
gpu-host = { path = "../../../crates/core/gpu-host", default-features = false }
cudarc = { version = "0.12", features = ["cuda-12060"] }
