[workspace]

[package]
name = "gpu-rag"
version = "0.1.0"
edition = "2021"
description = "GPU-Autonomous RAG: retrieval + GPT-2 generation in a single pipeline"

[dependencies]
cudarc = { version = "0.12", features = ["cuda-12060"] }
gpu-host = { path = "../../../crates/core/gpu-host", features = ["nn", "gpt2"], default-features = false }
