[workspace]

[package]
name = "graph-algorithms"
version = "0.1.0"
edition = "2021"
description = "Graph algorithms on GPU — BFS, PageRank with async scheduling"

[dependencies]
cudarc = { version = "0.12", features = ["cuda-12060"] }
