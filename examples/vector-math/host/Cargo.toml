[workspace]

[package]
name = "vector-math-host"
version = "0.1.0"
edition = "2021"
description = "Vector Math — pure GPU compute using gpu-host SDK"

[dependencies]
cudarc = { version = "0.12", features = ["cuda-12060"] }
gpu-host = { path = "../../../crates/core/gpu-host", default-features = false }
